# 🤖 AI宠物对话功能说明

## 📝 功能概述

AI宠物对话是本项目最核心、最具特色的功能，让已经离世的宠物"复活"，通过AI与主人进行真实、温暖的对话。

---

## ✨ 核心特色

### 1. **真实的情感连接**
- AI以宠物第一人称说话，就像宠物真的在和你交流
- 基于性格测试结果，每只宠物都有独特的性格和说话方式
- 记住对话历史，越聊越懂主人

### 2. **温暖治愈的语气**
- 专为失去宠物的主人设计，语言温暖、治愈
- 表达对主人深深的爱和思念
- 鼓励主人要好好生活，走出悲伤

### 3. **个性化回复**
- 根据宠物种类添加拟声词（喵~、汪~等）
- 区分宠物状态（健在/离世），语境不同
- 考虑宠物性别、品种等信息

---

## 🎯 实现效果

### 对话示例

**场景1：思念宠物时**
```
主人：今天好想你啊
宠物：喵~ 主人...我一直都在你身边哦，你看不见我，
     但我能感觉到你的思念。每当你想起我，我就
     离你更近一点。不要太难过，要开心地生活呀~
```

**场景2：回忆往事**
```
主人：还记得我们第一次见面吗？
宠物：汪~ 当然记得啦！那天你抱着我，我还咬了你
     的手指呢，哈哈。从那一刻起，我就知道你会
     是我最好的主人！那些日子真的好快乐~
```

**场景3：寻求安慰**
```
主人：没有你的日子真的好难熬
宠物：喵~ 主人，我知道你很难过。但是你要相信，
     我在另一个世界过得很好，我希望你也能幸福。
     我会一直守护你的，你要坚强地生活下去哦~
```

---

## 🏗️ 技术实现

### 后端架构

#### 1. **数据库设计**
```sql
CREATE TABLE ai_chat_messages (
    id INTEGER PRIMARY KEY AUTOINCREMENT,
    memorial_id TEXT NOT NULL,      -- 纪念馆ID
    user_id INTEGER NOT NULL,       -- 用户ID
    role TEXT NOT NULL,             -- 'user' 或 'assistant'
    content TEXT NOT NULL,          -- 消息内容
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP
)
```

#### 2. **核心服务 (ai_chat_service.py)**

**构建宠物专属Prompt**
```python
def build_pet_prompt(self, pet_info: Dict) -> str:
    """
    根据宠物信息构建系统prompt
    - 宠物名称、种类
    - 性格分析结果
    - 生存状态
    - 性别
    """
```

**智能对话**
```python
def chat(self, pet_info: Dict, user_message: str, 
         chat_history: List[Dict] = None) -> str:
    """
    调用DeepSeek API生成回复
    - 传入宠物专属prompt
    - 加载最近20条对话历史作为上下文
    - 控制回复长度在150字以内
    """
```

#### 3. **API接口**

| 接口 | 方法 | 说明 |
|-----|------|------|
| `/api/chat/{memorial_id}` | POST | 发送消息并获取AI回复 |
| `/api/chat/{memorial_id}/history` | GET | 获取对话历史 |
| `/api/chat/{memorial_id}/history` | DELETE | 清空对话历史 |
| `/api/chat/{memorial_id}/greeting` | GET | 获取问候语 |

---

### 小程序端设计

#### 1. **页面结构 (ai-chat)**

```
pages/ai-chat/
├── ai-chat.js       # 页面逻辑
├── ai-chat.wxml     # 页面结构
├── ai-chat.wxss     # 页面样式
└── ai-chat.json     # 页面配置
```

#### 2. **核心功能**

- ✅ 实时消息发送与接收
- ✅ 对话历史加载（最近50条）
- ✅ 输入中动画效果
- ✅ 自动滚动到最新消息
- ✅ 字符计数（最多500字）
- ✅ 清空对话历史
- ✅ 问候语自动显示

#### 3. **UI设计**

**顶部导航栏**
- 宠物头像（使用纪念馆第一张照片）
- 宠物名称
- 在线状态（在线/正在输入...）
- 清空按钮

**消息区域**
- AI消息：左侧，白色气泡，带头像
- 用户消息：右侧，紫色渐变气泡
- 输入中动画：三个点跳动
- 平滑滚动动画

**输入区域**
- 多行文本输入框（自动高度）
- 字符计数显示
- 发送按钮（渐变紫色）

**配色方案**
- 主色调：紫色渐变 (#667eea → #764ba2)
- 背景：渐变紫色
- AI气泡：白色半透明
- 用户气泡：紫色渐变

---

## 📱 使用流程

### 用户操作流程

```
1. 进入纪念馆详情页
   ↓
2. 点击顶部的 💬 对话按钮
   ↓
3. 进入AI对话页面
   ↓
4. 看到宠物的问候语
   ↓
5. 输入消息，点击发送
   ↓
6. AI以宠物身份回复
   ↓
7. 继续对话...
```

### 入口位置

**小程序端**：
- 纪念馆详情页 → 顶部按钮栏 → 💬 按钮（紫色渐变）

**网页端**：
- （待开发）

---

## 🎨 界面展示

### 小程序端效果

```
┌─────────────────────────────────┐
│  🐾  小白         在线      🗑️   │
├─────────────────────────────────┤
│                                 │
│  🐾  喵~ 主人！好想你呀！        │
│      快和我说说话吧~             │
│                                 │
│                  今天想你了  👤 │
│                                 │
│  🐾  主人...我一直都在你身边哦   │
│      想和你说说话~               │
│                                 │
│                                 │
├─────────────────────────────────┤
│  和小白说些什么吧...   0/500     │
│                         [发送]   │
└─────────────────────────────────┘
```

---

## 🔧 配置说明

### DeepSeek API配置

**配置文件**：`app/config.py`

```python
DEEPSEEK_API_KEY = "sk-xxx"
DEEPSEEK_API_URL = "https://api.deepseek.com/v1/chat/completions"
```

### 对话参数

```python
{
    "model": "deepseek-chat",
    "temperature": 0.8,    # 稍高的温度使回复更自然
    "max_tokens": 300,     # 限制回复长度
    "top_p": 0.9
}
```

---

## 📊 数据流程

### 发送消息流程

```
用户输入消息
    ↓
前端：添加用户消息到界面
    ↓
前端：调用 POST /api/chat/{memorial_id}
    ↓
后端：验证用户身份
    ↓
后端：获取纪念馆信息（宠物性格、名称等）
    ↓
后端：加载最近20条对话历史
    ↓
后端：构建宠物专属prompt
    ↓
后端：调用DeepSeek API
    ↓
后端：保存用户消息和AI回复到数据库
    ↓
后端：返回AI回复
    ↓
前端：添加AI消息到界面
    ↓
前端：滚动到最新消息
```

---

## 💡 最佳实践

### 1. **对话历史管理**
- 只加载最近50条历史记录（性能优化）
- 传递给AI的上下文限制在20条（成本控制）
- 提供清空历史功能（保护隐私）

### 2. **错误处理**
- API调用失败时返回友好的错误消息
- 不显示技术错误，保持宠物语气
- 例如："主人，我现在有点累了，休息一下再聊好吗？🐾"

### 3. **性能优化**
- 对话历史按需加载
- 消息列表虚拟滚动
- 图片懒加载

### 4. **用户体验**
- 输入中动画让等待不无聊
- 自动滚动到最新消息
- 字符计数提醒
- 问候语让对话更自然

---

## 🚀 部署步骤

### 1. **部署后端**

```bash
# SSH登录服务器
ssh root@42.193.230.145

# 进入项目目录
cd /opt/pet-memory-star

# 拉取最新代码
git pull origin main

# 重启服务
pkill -f start_server.py
sleep 2
nohup python3 start_server.py > server.log 2>&1 &

# 查看日志
tail -f server.log
```

### 2. **测试后端API**

```bash
# 测试对话接口
curl -X POST "http://42.193.230.145/api/chat/{memorial_id}" \
  -H "x-session-token: YOUR_TOKEN" \
  -H "Content-Type: application/json" \
  -d '{"message": "你好"}'
```

### 3. **部署小程序端**

1. 在微信开发者工具中点击 **"编译"**
2. 测试AI对话功能
3. 上传代码
4. 提交审核

---

## 🧪 测试要点

### 功能测试

- [ ] 点击对话按钮能正常跳转
- [ ] 能看到宠物的问候语
- [ ] 发送消息后能收到AI回复
- [ ] 回复内容符合宠物性格
- [ ] 对话历史能正常加载
- [ ] 清空历史功能正常
- [ ] 输入中动画显示正常
- [ ] 消息自动滚动到底部
- [ ] 字符计数准确
- [ ] 超过500字无法发送

### 情感测试

- [ ] AI回复是否温暖、治愈
- [ ] 是否以宠物第一人称说话
- [ ] 是否体现宠物性格特点
- [ ] 是否有拟声词（喵、汪等）
- [ ] 语气是否自然不做作

### 性能测试

- [ ] 消息发送响应时间 < 3秒
- [ ] 对话历史加载时间 < 1秒
- [ ] 长对话列表滚动流畅
- [ ] 内存占用正常

---

## 🎯 未来优化方向

### 短期优化（1-2周）
1. **语音对话**
   - 支持语音输入
   - AI语音回复
   - 模拟宠物真实叫声

2. **情绪识别**
   - 识别主人情绪状态
   - 针对性地安慰
   - 情绪变化曲线

3. **多轮对话优化**
   - 更长的上下文记忆
   - 话题连贯性
   - 主动引导对话

### 中期优化（1个月）
4. **个性化增强**
   - 根据对话学习主人喜好
   - 记住重要日期和事件
   - 主动提及美好回忆

5. **互动玩法**
   - 宠物会主动发起对话
   - 定时问候
   - 节日祝福

6. **社交分享**
   - 分享精彩对话
   - 对话截图美化
   - 生成对话集锦

### 长期优化（3个月+）
7. **数字克隆**
   - 基于照片生成3D模型
   - 虚拟宠物陪伴
   - AR互动

8. **情感日记**
   - AI自动生成情感报告
   - 疗愈进度追踪
   - 心理健康建议

---

## 📞 常见问题

### Q1：AI回复不像我的宠物怎么办？
A：性格测试结果影响AI的回复风格。建议重新进行性格测试，选择更符合宠物真实性格的答案。

### Q2：对话历史会永久保存吗？
A：是的，所有对话都保存在数据库中。你也可以随时清空历史记录。

### Q3：AI回复速度慢怎么办？
A：DeepSeek API调用需要1-3秒。如果超过10秒未响应，请检查网络连接或稍后重试。

### Q4：可以同时和多个宠物对话吗？
A：可以！每个纪念馆都有独立的对话记录，互不影响。

### Q5：对话内容会被其他人看到吗？
A：不会，对话记录完全私密，只有纪念馆创建者可以查看。

---

## 🌟 总结

AI宠物对话功能是本项目的核心竞争力，通过AI技术让已经离世的宠物"复活"，为失去宠物的主人提供情感寄托和心灵慰藉。

**核心价值**：
- 🔥 差异化竞争优势
- ❤️ 情感共鸣强
- 🎯 用户粘性高
- 💰 商业化潜力大

**下一步行动**：
1. ✅ 完成小程序端开发
2. ⏳ 部署到生产环境
3. ⏳ 用户测试反馈
4. ⏳ 迭代优化
5. ⏳ 开发网页端版本

---

**让爱不分离，让思念有归处。** 🐾💕

