# ⚠️ 紧急修复步骤

**时间**: 2025-10-21 10:58  
**问题**: API 500 错误 + 密码明文显示

---

## 🚨 问题分析

### 问题 1: API 500 错误（严重）
```
GET http://42.193.230.145/api/user/level-info 500 (Internal Server Error)
GET http://42.193.230.145/api/user/dashboard 500 (Internal Server Error)
```

**原因**: 服务器代码未更新，仍在使用旧版本的 API

**影响**: 
- 首页加载失败
- 用户体验差
- 后续功能可能受影响

---

### 问题 2: 密码明文显示
**代码确认**: `login.wxml` 第 32 行已经设置 `type="password"`

**可能原因**:
1. 开发者工具缓存未清除干净
2. 真机调试可能不会有这个问题
3. 需要强制重新编译

---

## ✅ 修复步骤（请按顺序执行）

### 第 1 步：更新服务器代码（最重要！）

#### 方法 A：使用一键脚本（推荐）
```bash
# 1. SSH 连接服务器
ssh root@42.193.230.145

# 2. 执行更新脚本
cd /opt/pet-memory-star
bash 快速更新服务器.sh
```

#### 方法 B：手动执行
```bash
# SSH 连接
ssh root@42.193.230.145

# 进入目录
cd /opt/pet-memory-star

# 拉取代码
git fetch --all
git reset --hard origin/main

# 停止服务
pkill -f uvicorn

# 启动服务
source venv/bin/activate
nohup python3 start_server.py > nohup.out 2>&1 &

# 等待启动
sleep 10

# 验证
ps aux | grep python
curl http://localhost/
```

**验证服务器更新成功**:
```bash
# 查看最新提交
git log -1 --oneline
# 应该看到: Enhance: Improve error handling...

# 查看日志
tail -20 nohup.out
# 应该看到: Application startup complete
```

---

### 第 2 步：清除小程序缓存（彻底清除）

#### 方法 A：完全重启（最彻底）
1. **保存所有文件** (Ctrl+S)
2. **完全关闭**微信开发者工具（不是最小化！）
3. **删除临时文件**:
   - Windows: `C:\Users\你的用户名\AppData\Local\微信开发者工具\`
   - 删除 `User Data\Default\Cache` 文件夹
4. **重新打开**微信开发者工具
5. 打开项目
6. **清除缓存**: 工具 → 清除缓存 → 清除所有缓存
7. **重新编译**

#### 方法 B：强制刷新（快速）
1. 工具 → 清除缓存 → 清除所有缓存
2. 工具 → 清除缓存 → 清除文件缓存
3. 工具 → 清除缓存 → 清除授权数据
4. Ctrl+Shift+F5 (强制刷新)
5. 点击 "编译"

---

### 第 3 步：验证修复

#### 验证密码隐藏
1. 打开登录页
2. **在密码输入框中输入任意字符**
3. **应该显示为 `••••••`**，而不是明文

**如果仍然是明文**:
- 这是开发者工具的 BUG
- **在真机上测试**（真机不会有这个问题）
- 代码本身是正确的

#### 验证 API 正常
1. 登录成功后进入首页
2. 打开控制台
3. **应该看到**:
   ```
   首页检查登录状态: {hasUserInfo: true, ...}
   ```
4. **不应该看到大量 500 错误**
5. 如果有错误，应该是：
   ```
   ⚠️ 等级信息加载失败，使用默认值
   ⚠️ 统计数据加载失败，使用默认值
   ```
   这是正常的，说明我们的错误处理生效了

---

## 🔍 详细验证清单

### 服务器端
```bash
# 1. 检查 Git 版本
cd /opt/pet-memory-star
git log -3 --oneline
# 应该包含:
# - Enhance: Improve error handling...
# - Docs: Add deployment guide...
# - Fix: Remove explicit port 80...

# 2. 检查服务运行
ps aux | grep python
# 应该看到: python3 start_server.py

# 3. 检查端口
ss -tulpn | grep :80
# 应该看到: LISTEN 0.0.0.0:80

# 4. 测试 API（需要真实 token）
curl -X GET http://localhost/api/user/dashboard \
  -H "Authorization: Bearer YOUR_SESSION_TOKEN" \
  -H "Content-Type: application/json"
```

### 小程序端
1. **登录测试**:
   - ✅ 能成功登录
   - ✅ 显示 "登录成功！"
   - ✅ 1.5秒后跳转到首页

2. **首页测试**:
   - ✅ 显示 "欢迎回来！"
   - ✅ 显示邮箱地址
   - ✅ 显示操作按钮
   - ✅ 控制台没有大量连续的 500 错误

3. **密码测试**:
   - ✅ 密码输入框显示为 `••••••`
   - 如果是明文：在真机上测试

---

## ⏱️ 预计时间

- 服务器更新: **2-3 分钟**
- 清除缓存: **1 分钟**
- 测试验证: **2-3 分钟**

**总计**: 约 **5-7 分钟**

---

## 📞 遇到问题？

### 服务器无法连接
```bash
# 测试连接
ping 42.193.230.145

# 测试 SSH
ssh -v root@42.193.230.145
```

### Git 拉取失败
```bash
# 检查远程
git remote -v

# 强制重置
git fetch --all
git reset --hard origin/main
```

### 服务无法启动
```bash
# 查看错误日志
tail -50 /opt/pet-memory-star/nohup.out

# 检查端口占用
ss -tulpn | grep :80

# 杀死占用进程
pkill -9 -f uvicorn
```

---

## 🎯 预期结果

### 修复完成后，应该看到：

**服务器端**:
```bash
[root@VM-24-14-opencloudos pet-memory-star]# git log -1 --oneline
9389f55 Docs: Add deployment guide, test checklist, and quick update script

[root@VM-24-14-opencloudos pet-memory-star]# ps aux | grep python
root     12345  python3 start_server.py

[root@VM-24-14-opencloudos pet-memory-star]# ss -tulpn | grep :80
LISTEN 0.0.0.0:80
```

**小程序端**:
- 登录成功 ✅
- 首页正常 ✅
- API 错误减少 ✅
- 有默认值显示 ✅

---

**执行人**: _____________  
**执行时间**: _____________  
**修复状态**: ⬜ 成功 ⬜ 部分成功 ⬜ 失败

**备注**:
```
（记录遇到的问题和解决方法）
```

