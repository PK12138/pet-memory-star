# ✅ 完整测试清单

**更新时间**: 2025-10-21  
**版本**: v3.0

---

## 🔧 服务器端测试

### 1. 代码更新
```bash
# 在服务器上执行
cd /opt/pet-memory-star
bash 快速更新服务器.sh
```

**预期结果**:
- ✅ 代码成功拉取
- ✅ 服务自动重启
- ✅ 端口 80 正常监听
- ✅ API 测试通过

---

## 📱 小程序端测试

### 前置准备
1. **清除缓存** (重要！)
   - 微信开发者工具 → 工具 → 清除缓存
   - 选择 "清除所有缓存"
   - 完全关闭开发者工具
   - 重新打开项目

2. **确认配置**
   - 检查 `app.js` 中 baseUrl 是否为: `http://42.193.230.145`
   - 检查控制台输出的配置版本号

---

### 测试 1: 登录功能
**步骤**:
1. 打开小程序，点击 "登录"
2. 输入邮箱: `1208155205@qq.com`
3. 输入密码
4. 点击 "登录" 按钮

**预期结果**:
- ✅ 显示 "登录成功！" 提示
- ✅ 1.5 秒后自动跳转到首页
- ✅ 密码输入框显示为 `•••` (已隐藏)
- ✅ 控制台输出:
  ```
  登录成功响应: {"success":true,...}
  app.login() 被调用: {...}
  准备跳转到首页，延迟1.5秒
  执行跳转到首页
  跳转成功
  ```

**可能的问题**:
- ❌ 如果看到 "网络错误"，检查 baseUrl 配置
- ❌ 如果跳转失败，查看控制台错误信息

---

### 测试 2: 首页显示
**步骤**:
1. 登录成功后，自动进入首页
2. 观察页面显示

**预期结果**:
- ✅ 显示 "欢迎回来！"
- ✅ 显示用户邮箱: `1208155205@qq.com`
- ✅ 显示 "开始创建纪念馆" 按钮
- ✅ 显示快速操作区域（我的纪念馆、照片管理等）
- ✅ 即使 API 部分失败，也显示默认数据（不再显示空白）

**控制台输出**:
```
首页 onLoad() 被调用
首页检查登录状态: {hasUserInfo: true, hasSessionToken: true, ...}
```

**如果 API 失败**:
```
⚠️ 等级信息加载失败，使用默认值
⚠️ 统计数据加载失败，使用默认值
```
**这是正常的** - 不影响登录状态，页面仍显示默认数据。

---

### 测试 3: 性格测试 - 基本信息
**步骤**:
1. 首页点击 "开始创建纪念馆"
2. 进入性格测试页面
3. 填写宠物基本信息:
   - 宠物名字: `小白`
   - 物种: `狗`
   - 品种: `金毛`
   - 颜色: `金黄色`
   - 性别: `公`
   - 体重: `25`
   - 出生日期: 选择日期
   - 纪念日期: 选择日期
   - 状态: `健在`
   - 地址: `北京市朝阳区`

**预期结果**:
- ✅ 所有输入框清晰可见，字体大小合适
- ✅ 必填项有红色 `*` 标记
- ✅ 填写完所有必填项后，"开始性格测试" 按钮变为可点击
- ✅ 点击按钮后，进入性格测试题目

---

### 测试 4: 性格测试 - 答题
**步骤**:
1. 基本信息填写完成后，点击 "开始性格测试"
2. 回答 10 道性格测试题
3. 每题选择一个选项后，点击 "下一题"

**预期结果**:
- ✅ 题目和选项正常显示
- ✅ 进度条正常更新 (1/10 → 2/10 → ... → 10/10)
- ✅ 第 10 题时，按钮文字变为 "完成测试"
- ✅ 点击 "完成测试" 后，显示纪念馆描述输入框

**关键点**:
- 进度条应该在答题过程中可见
- 填写基本信息时，进度条应该隐藏

---

### 测试 5: 创建纪念馆
**步骤**:
1. 完成性格测试后
2. 输入纪念馆描述（至少几个字）
3. 点击 "创建纪念馆" 按钮

**预期结果**:
- ✅ 显示加载状态 "创建中..."
- ✅ 控制台输出:
  ```
  开始创建纪念馆，宠物信息: {...}
  创建纪念馆响应: {"success": true, ...}
  ```
- ✅ 显示 "纪念馆创建成功" 提示
- ✅ 1.5 秒后跳转到纪念馆列表页

**可能的问题**:
- ❌ 如果显示 "创建失败"，查看控制台错误信息
- ❌ 如果显示 "未提供有效的认证令牌"，说明登录状态丢失，需要重新登录

---

## 🔍 调试技巧

### 1. 查看请求详情
在微信开发者工具中:
1. 打开 "调试器" → "Network" 标签
2. 执行操作（如登录、创建纪念馆）
3. 查看请求和响应

**关键请求**:
- `POST /api/auth/login` - 登录
- `GET /api/user/dashboard` - 仪表板数据
- `GET /api/user/level-info` - 等级信息
- `POST /api/memorial/create` - 创建纪念馆

### 2. 查看本地存储
控制台执行:
```javascript
// 查看 sessionToken
console.log(wx.getStorageSync('sessionToken'))

// 查看 userInfo
console.log(wx.getStorageSync('userInfo'))

// 查看全局数据
console.log(getApp().globalData)
```

### 3. 清除登录状态（如需重新测试登录）
控制台执行:
```javascript
wx.clearStorageSync()
wx.reLaunch({url: '/pages/index/index'})
```

---

## 🐛 常见问题排查

### 问题 1: 登录成功但无法跳转
**症状**: 显示 "登录成功！" 但停留在登录页

**排查**:
1. 查看控制台是否有跳转错误
2. 检查 `app.js` 中的 `baseUrl` 配置
3. 确认 `app.login()` 是否被调用

**解决**:
- 清除缓存，重新编译
- 确保使用最新代码

---

### 问题 2: 首页显示 "登录失败"
**症状**: 登录成功后，首页显示异常或提示登录失败

**原因**: 这不是真正的登录失败，而是 API 加载错误的误导

**验证**:
1. 查看控制台，确认 `sessionToken` 存在
2. 查看是否有 "等级信息加载失败" 或 "统计数据加载失败" 的警告

**解决**:
- ✅ 已在本次更新中修复
- API 错误现在使用默认值，不影响登录状态

---

### 问题 3: 创建纪念馆失败
**症状**: 点击 "创建纪念馆" 后显示失败

**排查**:
1. 查看控制台错误信息
2. 检查 Network 标签，查看 `/api/memorial/create` 请求
3. 查看请求的 Headers 是否包含 `Authorization: Bearer <token>`

**常见错误**:
- `401 Unauthorized` → 重新登录
- `403 Forbidden` → 检查用户权限
- `500 Internal Server Error` → 查看服务器日志

**服务器端排查**:
```bash
# 查看服务器日志
tail -f /opt/pet-memory-star/nohup.out

# 应该能看到:
# 📝 收到创建纪念馆请求，数据: {...}
# 🐾 宠物信息: {...}
# ✅ 纪念馆创建成功: /memorial/xxx
```

---

### 问题 4: 配置未更新（仍使用旧 baseUrl）
**症状**: 改了代码，但小程序仍使用旧配置

**解决步骤**:
1. **完全关闭微信开发者工具** (不是刷新！)
2. 删除项目目录下的 `.cache` 文件夹（如果有）
3. 重新打开微信开发者工具
4. 点击 "清除缓存" → "清除所有缓存"
5. 点击 "编译"
6. 查看控制台，确认配置版本号

**确认配置已更新**:
控制台应该显示:
```
========================================
🔧 小程序配置信息 [v2.0]
📡 baseUrl: http://42.193.230.145
🆔 appId: wx9572f66945407446
⏰ 配置时间: 2024-10-20 23:30
========================================
```

---

## 📊 测试记录

| 测试项 | 状态 | 测试时间 | 备注 |
|--------|------|----------|------|
| 登录功能 | ⬜ 待测试 | - | - |
| 首页显示 | ⬜ 待测试 | - | - |
| 性格测试 - 基本信息 | ⬜ 待测试 | - | - |
| 性格测试 - 答题 | ⬜ 待测试 | - | - |
| 创建纪念馆 | ⬜ 待测试 | - | - |
| API 错误处理 | ⬜ 待测试 | - | - |

**说明**: 请在测试完成后，将 `⬜ 待测试` 更改为 `✅ 通过` 或 `❌ 失败`

---

## 🎯 测试完成标准

所有以下项都应该通过:
- [ ] 登录成功并正确跳转
- [ ] 密码输入已隐藏
- [ ] 首页显示用户信息
- [ ] 首页 API 错误不影响登录状态
- [ ] 性格测试页面所有字段清晰可见
- [ ] 性格测试流程完整（10 道题）
- [ ] 创建纪念馆成功

---

**测试人员**: _____________  
**测试日期**: _____________  
**测试结果**: ⬜ 全部通过 ⬜ 部分通过 ⬜ 失败

**问题记录**:
```
（如有问题，请在此记录详细信息）
```

