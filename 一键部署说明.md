# 🚀 一键部署说明

## ✅ 代码已推送到Gitee

提交信息：`修复小程序功能: 添加创建纪念馆API、修复性格测试题目显示和完成按钮、优化登录跳转逻辑`

Git仓库：`https://gitee.com/PK12138/pet-memory-star.git`

---

## 📋 在服务器执行部署

### 1. SSH登录服务器

```bash
ssh root@42.193.230.145
```

### 2. 执行一键部署脚本

```bash
bash /opt/deploy.sh
```

---

## 🔍 如果没有 deploy.sh，手动部署步骤

### 方法1：Git拉取 + 重启

```bash
# 进入项目目录
cd /opt/pet-memory-star

# 拉取最新代码
git pull origin main

# 停止旧服务
pkill -9 -f uvicorn
sleep 2

# 重启服务
source venv/bin/activate
nohup python start_server.py > server.log 2>&1 &

# 查看日志确认启动成功
tail -30 server.log
```

### 方法2：创建 deploy.sh 脚本（如果不存在）

```bash
# 创建部署脚本
cat > /opt/deploy.sh << 'EOF'
#!/bin/bash

echo "🚀 开始部署 Pet Memory Star..."

# 项目路径
PROJECT_DIR="/opt/pet-memory-star"

# 切换到项目目录
cd $PROJECT_DIR

# 拉取最新代码
echo "📥 拉取最新代码..."
git pull origin main

# 检查是否拉取成功
if [ $? -ne 0 ]; then
    echo "❌ Git拉取失败！"
    exit 1
fi

# 停止旧服务
echo "⏹️  停止旧服务..."
pkill -9 -f uvicorn
sleep 2

# 激活虚拟环境
echo "🔧 激活虚拟环境..."
source venv/bin/activate

# 安装/更新依赖（如果需要）
# pip install -r requirements.txt

# 启动新服务
echo "▶️  启动新服务..."
nohup python start_server.py > server.log 2>&1 &

# 等待服务启动
sleep 3

# 检查服务状态
if pgrep -f uvicorn > /dev/null; then
    echo "✅ 服务启动成功！"
    echo ""
    echo "📊 最新日志："
    tail -20 server.log
    echo ""
    echo "🎉 部署完成！"
else
    echo "❌ 服务启动失败！"
    echo ""
    echo "📋 错误日志："
    tail -50 server.log
    exit 1
fi
EOF

# 给脚本执行权限
chmod +x /opt/deploy.sh

# 执行部署
bash /opt/deploy.sh
```

---

## ✅ 部署验证

部署完成后，检查以下内容：

### 1. 查看服务状态

```bash
# 检查进程
ps aux | grep uvicorn

# 查看日志
tail -50 /opt/pet-memory-star/server.log
```

### 2. 测试健康检查

```bash
curl http://localhost:8000/api/health
```

应该返回：
```json
{
  "status": "healthy",
  "message": "服务器运行正常",
  "timestamp": "..."
}
```

### 3. 测试新API

```bash
# 检查新API端点是否存在
curl http://localhost:8000/docs
```

在API文档中应该能看到 `POST /api/memorial/create`

---

## 📊 本次更新内容

### 🔧 后端修改

1. **新增API** `POST /api/memorial/create`
   - 文件：`app/main.py`
   - 功能：小程序创建纪念馆（JSON格式）
   - 位置：第283-360行

### 🔧 小程序修改

1. **性格测试题目显示**
   - 文件：`miniprogram/pages/personality-test/personality-test.js`
   - 修复：使用本地题目数据，10道完整题目

2. **性格测试完成按钮**
   - 文件：`miniprogram/pages/personality-test/personality-test.wxml`
   - 修复：第10题显示"完成测试"按钮
   - 修改：`currentQuestion < totalQuestions` → `currentQuestion <= totalQuestions`

3. **登录后跳转**
   - 文件：`miniprogram/pages/login/login.js`
   - 修复：使用 `wx.reLaunch()` 替代 `wx.switchTab()`

4. **服务器地址配置**
   - 文件：`miniprogram/app.js`, `miniprogram/utils/api.js`
   - 配置：`baseUrl: 'http://42.193.230.145'`

### 📝 新增文档

1. `创建纪念馆API修复.md` - API修复说明
2. `性格测试题目修复.md` - 题目显示修复
3. `小程序与网页版功能对比.md` - 功能完整对比
4. `部署新API到服务器.md` - 部署指南
5. 其他修复文档...

---

## 🎯 部署后测试

### 1. 重新编译小程序

在微信开发者工具中：
1. 工具 → 清除缓存 → 清除全部
2. 重新编译

### 2. 测试创建纪念馆流程

1. 登录小程序
2. 进入性格测试
3. 填写宠物信息
4. 完成10道题目
5. 点击"完成测试"
6. 查看性格分析结果
7. 填写纪念馆描述
8. **点击"创建纪念馆"**

### 3. 预期结果

**Console输出**：
```
✅ POST http://42.193.230.145/api/memorial/create 200 OK
✅ 响应: {success: true, message: "纪念馆创建成功", ...}
✅ 创建成功，跳转到纪念馆列表
```

**而不是**：
```
❌ POST http://42.193.230.145/api/memorial/create 404 (Not Found)
```

---

## 🔍 故障排查

### 问题1：Git拉取失败

```bash
# 检查Git状态
cd /opt/pet-memory-star
git status

# 强制拉取
git fetch --all
git reset --hard origin/main
```

### 问题2：服务启动失败

```bash
# 查看详细日志
tail -100 /opt/pet-memory-star/server.log

# 检查端口占用
netstat -tulpn | grep 8000

# 手动启动查看错误
cd /opt/pet-memory-star
source venv/bin/activate
python start_server.py
```

### 问题3：新API不生效

```bash
# 确认代码已更新
cat /opt/pet-memory-star/app/main.py | grep -A 5 "api/memorial/create"

# 确认服务进程
ps aux | grep uvicorn

# 重启服务
pkill -9 -f uvicorn
cd /opt/pet-memory-star
source venv/bin/activate
nohup python start_server.py > server.log 2>&1 &
```

---

## 📞 需要帮助？

如果部署过程中遇到问题：

1. **查看日志**：`tail -100 /opt/pet-memory-star/server.log`
2. **检查进程**：`ps aux | grep uvicorn`
3. **测试API**：`curl http://localhost:8000/api/health`
4. **查看端口**：`netstat -tulpn | grep 8000`

---

**✨ 现在你可以在服务器上执行 `bash /opt/deploy.sh` 来一键部署了！**

_创建时间: 2025-01-14_

