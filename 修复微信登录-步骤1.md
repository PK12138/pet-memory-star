# 🔧 修复微信登录问题 - 步骤1

## 📋 问题描述

微信登录返回 500 错误，错误信息：
```
AttributeError: 'Database' object has no attribute 'get_user_by_openid'
```

## ✅ 已完成的修复

1. ✅ 在 `database.py` 中添加了 `get_user_by_openid` 方法
2. ✅ 在 `database.py` 中添加了 `create_user_by_openid` 方法
3. ✅ 修改了 `users` 表结构，添加了 `openid`, `nickname`, `phone` 字段
4. ✅ 创建了数据库迁移脚本 `migrate_add_openid.py`

## 🚀 执行步骤

### 方式1：自动迁移（推荐）

在项目根目录执行：

```bash
# Windows
python migrate_add_openid.py

# Linux/Mac
python3 migrate_add_openid.py
```

### 方式2：手动迁移（如果自动迁移失败）

连接到数据库并执行 SQL：

```bash
# 进入项目目录
cd /opt/pet-memory-star

# 连接数据库
sqlite3 app/pet_memorials.db
```

然后执行以下 SQL：

```sql
-- 添加 openid 字段
ALTER TABLE users ADD COLUMN openid TEXT UNIQUE;

-- 添加 nickname 字段
ALTER TABLE users ADD COLUMN nickname TEXT;

-- 添加 phone 字段
ALTER TABLE users ADD COLUMN phone TEXT;

-- 查看表结构确认
PRAGMA table_info(users);

-- 退出
.quit
```

## 🔍 验证修复

### 1. 检查数据库字段

```bash
sqlite3 app/pet_memorials.db "PRAGMA table_info(users);"
```

应该看到 `openid`, `nickname`, `phone` 字段。

### 2. 重启服务

```bash
# 使用一键部署脚本
bash 快速更新服务器.sh

# 或手动重启
pkill -f "python.*start_server.py"
cd /opt/pet-memory-star
source venv/bin/activate
nohup python3 start_server.py > nohup.out 2>&1 &
```

### 3. 测试微信登录

在小程序中点击"微信一键登录"，应该能正常登录了。

### 4. 查看日志

```bash
tail -f /opt/pet-memory-star/app.log
# 或
tail -f /opt/pet-memory-star/nohup.out
```

应该看到：
- ✅ 获取到openid: xxx
- 👤 用户已存在 或 🆕 首次登录，创建新用户
- 🎫 创建会话成功

## ⚠️ 注意事项

1. **备份数据库**（重要！）
   ```bash
   cp app/pet_memorials.db app/pet_memorials.db.backup
   ```

2. **字段已存在的情况**
   - 如果字段已存在，迁移脚本会自动跳过，不会报错

3. **UNIQUE 约束**
   - `openid` 字段设置为 UNIQUE，确保每个微信用户只有一个账号

## 📝 修改的文件

1. `app/database.py`
   - 添加了 `create_user_by_openid` 方法
   - 优化了 `get_user_by_openid` 方法
   - 修改了 `_create_tables` 方法，自动添加字段

2. `migrate_add_openid.py`（新建）
   - 数据库迁移脚本

## 🎯 下一步

修复完成后，可以继续：
- 步骤2：实现反馈功能的后端 API
- 步骤3：优化首页体验

---

**修复完成后，微信登录应该可以正常工作了！** 🎉

