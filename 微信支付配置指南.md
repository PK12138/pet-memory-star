# 微信小程序支付配置指南

本文档详细说明如何配置微信小程序支付功能。

## 📋 前提条件

1. **小程序已认证**：必须是已认证的小程序
2. **开通微信支付**：在微信支付商户平台开通支付功能
3. **服务器域名备案**：域名需要ICP备案
4. **HTTPS证书**：小程序要求必须使用HTTPS

## 🔑 一、获取必要的参数

### 1.1 小程序AppID

登录[微信公众平台](https://mp.weixin.qq.com/)，在「开发」-「开发管理」-「开发设置」中查看：

```
WECHAT_APP_ID=wx9572f66945407446
```

### 1.2 商户号信息

登录[微信支付商户平台](https://pay.weixin.qq.com/)，获取以下信息：

**商户号（mchid）**：
- 位置：账户中心 → 商户信息
- 示例：`1234567890`

**API密钥（API Key）**：
- 位置：账户中心 → API安全 → API密钥
- 长度：32位字符串
- ⚠️ 注意：API v2版本的密钥（如果使用v2接口）

**API v3密钥**：
- 位置：账户中心 → API安全 → APIv3密钥
- 长度：32位字符串
- ⚠️ 用于v3版本接口的签名验证

```bash
WECHAT_MCH_ID=1234567890
WECHAT_API_KEY=your_api_v2_key_32_characters
WECHAT_API_V3_KEY=your_api_v3_key_32_characters
```

### 1.3 API证书

下载商户API证书（用于v3接口签名）：

1. 登录商户平台
2. 进入「账户中心」→「API安全」→「申请API证书」
3. 下载证书工具并生成证书
4. 保存以下文件：
   - `apiclient_cert.pem` - 商户证书
   - `apiclient_key.pem` - 商户私钥
   - 证书序列号（Certificate Serial Number）

```bash
WECHAT_CERT_SERIAL_NO=证书序列号（大写，无冒号）
WECHAT_PRIVATE_KEY_PATH=/opt/pet-memory-star/certs/apiclient_key.pem
WECHAT_CERT_PATH=/opt/pet-memory-star/certs/apiclient_cert.pem
```

## 🚀 二、服务器端配置

### 2.1 上传证书到服务器

```bash
# 在本地电脑创建证书文件夹并放入证书
mkdir -p certs

# 将证书上传到服务器
scp certs/* root@42.193.230.145:/opt/pet-memory-star/certs/

# 设置证书权限（重要！）
ssh root@42.193.230.145
cd /opt/pet-memory-star/certs
chmod 600 *.pem
ls -la  # 确认权限正确
```

### 2.2 配置环境变量

在服务器上编辑 `.env` 文件：

```bash
cd /opt/pet-memory-star
vim .env
```

添加以下配置：

```bash
# 微信小程序配置
WECHAT_APP_ID=wx9572f66945407446
WECHAT_APP_SECRET=你的小程序AppSecret

# 微信支付配置
WECHAT_MCH_ID=你的商户号
WECHAT_API_KEY=你的APIv2密钥
WECHAT_API_V3_KEY=你的APIv3密钥
WECHAT_CERT_SERIAL_NO=你的证书序列号
WECHAT_PRIVATE_KEY_PATH=/opt/pet-memory-star/certs/apiclient_key.pem
WECHAT_CERT_PATH=/opt/pet-memory-star/certs/apiclient_cert.pem

# 支付回调地址（必须是HTTPS）
SERVER_BASE_URL=https://pettrailstar.cn
WECHAT_NOTIFY_URL=https://pettrailstar.cn/api/payment/wechat/notify

# 数据库配置
DATABASE_PATH=/opt/pet-memory-star/pet_memorials.db

# 存储配置
STORAGE_PATH=/opt/pet-memory-star/storage
```

### 2.3 配置微信支付商户平台

1. **配置支付目录**：
   - 登录微信支付商户平台
   - 进入「产品中心」→「开发配置」
   - 添加支付授权目录：`https://pettrailstar.cn/pages/payment/`
   - ⚠️ 必须是HTTPS，必须以`/`结尾

2. **配置回调通知地址**（如需要）：
   - 在创建订单时会动态传入回调地址
   - 确保服务器防火墙允许微信服务器访问

3. **配置域名白名单**：
   - 在小程序管理后台配置服务器域名
   - request合法域名：`https://pettrailstar.cn`
   - uploadFile合法域名：`https://pettrailstar.cn`

## 🔧 三、小程序端配置

### 3.1 配置服务器域名

登录[微信公众平台](https://mp.weixin.qq.com/)：

1. 进入「开发」→「开发管理」→「开发设置」→「服务器域名」
2. 添加以下域名：
   - **request合法域名**：`https://pettrailstar.cn`
   - **uploadFile合法域名**：`https://pettrailstar.cn`
   - **downloadFile合法域名**：`https://pettrailstar.cn`

### 3.2 检查代码配置

确保 `miniprogram/app.js` 和 `miniprogram/utils/api.js` 中的 `baseUrl` 正确：

```javascript
// miniprogram/app.js
App({
  baseUrl: 'https://pettrailstar.cn',  // 必须是HTTPS
  // ...
})

// miniprogram/utils/api.js
const baseUrl = 'https://pettrailstar.cn'  // 必须是HTTPS
```

## ✅ 四、测试支付流程

### 4.1 沙箱环境测试（可选）

微信支付提供沙箱环境用于测试：

```bash
# 在.env中添加
WECHAT_SANDBOX=true  # 使用沙箱环境
```

### 4.2 生产环境测试

1. **测试用户登录**：
   - 使用微信扫码登录小程序
   - 确认用户信息正确

2. **测试创建订单**：
   - 在小程序中进入「会员中心」
   - 选择套餐
   - 选择「微信支付」
   - 检查控制台日志

3. **测试支付流程**：
   - 调用 `wx.requestPayment`
   - 完成支付
   - 检查订单状态

### 4.3 查看日志

```bash
# 在服务器查看实时日志
ssh root@42.193.230.145
tail -f /opt/pet-memory-star/app.log

# 查看支付相关日志
grep "支付" /opt/pet-memory-star/app.log | tail -50
grep "payment" /opt/pet-memory-star/app.log | tail -50

# 查看错误日志
grep "ERROR" /opt/pet-memory-star/app.log | tail -20
```

## ⚠️ 五、常见问题

### 5.1 支付失败：签名错误

**原因**：
- API密钥配置错误
- 证书序列号错误
- 证书文件权限不对
- 使用了错误的签名方法

**解决方案**：
```bash
# 1. 检查证书权限
ls -la /opt/pet-memory-star/certs/

# 2. 检查环境变量
cat /opt/pet-memory-star/.env | grep WECHAT

# 3. 重新生成证书序列号（去掉冒号，全大写）
# 示例：1A:2B:3C:4D → 1A2B3C4D
```

### 5.2 支付失败：缺少openid

**原因**：
- 用户未通过微信登录
- 用户信息中没有openid字段
- 数据库中openid为空

**解决方案**：
```sql
-- 检查用户openid
sqlite3 /opt/pet-memory-star/pet_memorials.db
SELECT id, email, nickname, openid FROM users LIMIT 10;

-- 确保用户通过微信登录重新获取openid
```

### 5.3 支付失败：不合法的请求

**原因**：
- 小程序AppID与商户号不匹配
- 商户号未开通小程序支付
- 支付金额格式错误（必须是整数分）

**解决方案**：
1. 确认商户号与AppID绑定关系
2. 在商户平台开通「小程序支付」产品
3. 检查金额转换逻辑（元→分）

### 5.4 回调通知失败

**原因**：
- 回调URL不是HTTPS
- 服务器防火墙阻止微信服务器
- 回调接口返回格式不正确

**解决方案**：
```bash
# 1. 确保使用HTTPS
echo $SERVER_BASE_URL  # 应该是https://

# 2. 测试回调接口
curl -X POST https://pettrailstar.cn/api/payment/wechat/notify \
  -H "Content-Type: application/json" \
  -d '{"test":"data"}'

# 3. 查看防火墙规则
firewall-cmd --list-all
```

## 📊 六、监控和维护

### 6.1 日志监控

定期检查支付相关日志：

```bash
# 每日支付统计
grep "支付成功" /opt/pet-memory-star/app.log | wc -l

# 查看失败的支付
grep "支付失败" /opt/pet-memory-star/app.log | tail -20

# 查看异常订单
sqlite3 /opt/pet-memory-star/pet_memorials.db \
  "SELECT * FROM payment_orders WHERE status='pending' AND created_at < datetime('now', '-1 hour');"
```

### 6.2 证书更新

API证书有效期通常为1年，需要定期更新：

```bash
# 1. 下载新证书
# 2. 备份旧证书
cp /opt/pet-memory-star/certs/apiclient_key.pem /opt/pet-memory-star/certs/apiclient_key.pem.bak

# 3. 上传新证书
scp new_cert/* root@42.193.230.145:/opt/pet-memory-star/certs/

# 4. 更新证书序列号
vim /opt/pet-memory-star/.env  # 更新WECHAT_CERT_SERIAL_NO

# 5. 重启服务
cd /opt/pet-memory-star
bash /opt/deploy.sh
```

## 🔐 七、安全建议

1. **保护敏感信息**：
   ```bash
   # 设置.env文件权限
   chmod 600 /opt/pet-memory-star/.env
   
   # 不要提交.env到Git
   echo ".env" >> .gitignore
   ```

2. **定期更换密钥**：
   - 每3-6个月更换API密钥
   - 更换后更新环境变量并重启服务

3. **监控异常交易**：
   - 设置金额上限
   - 监控频繁失败的支付
   - 及时处理退款申请

4. **备份重要数据**：
   ```bash
   # 备份数据库
   sqlite3 /opt/pet-memory-star/pet_memorials.db .dump > backup.sql
   
   # 备份证书
   tar -czf certs_backup.tar.gz /opt/pet-memory-star/certs/
   ```

## 📞 八、联系支持

- **微信支付技术支持**：[https://kf.qq.com/product/wechatpaymentmerchant.html](https://kf.qq.com/product/wechatpaymentmerchant.html)
- **微信小程序文档**：[https://developers.weixin.qq.com/miniprogram/dev/framework/](https://developers.weixin.qq.com/miniprogram/dev/framework/)
- **微信支付API文档**：[https://pay.weixin.qq.com/wiki/doc/apiv3/index.shtml](https://pay.weixin.qq.com/wiki/doc/apiv3/index.shtml)

---

**最后更新**：2025-11-01
**维护者**：Pet Memory Star Team

