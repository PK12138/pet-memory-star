# 🚀 快速修复命令 - 更新服务器代码

## 问题
服务器代码版本过旧，缺少 `/api/memorial/create` API端点

## 解决方案

### 在服务器上执行以下命令：

```bash
cd /opt/pet-memory-star && \
git fetch --all && \
git reset --hard origin/main && \
pkill -f uvicorn && \
source venv/bin/activate && \
nohup python3 start_server.py > server.log 2>&1 & \
sleep 5 && \
echo "✅ 验证API端点..." && \
curl -k https://localhost:443/api/memorial/create -X POST -H "Content-Type: application/json" -d '{"test":"data"}' && \
echo "" && \
echo "📝 当前代码版本:" && \
git log -1 --oneline
```

## 预期结果

1. **Git更新**：`HEAD is now at 50567be`
2. **API测试**：返回 `{"detail":"Not authenticated"}` （而不是 `{"detail":"Not Found"}`）
3. **代码版本**：`50567be Feature: Add /api/memorial/create endpoint for Mini Program`

## 如果还是404

检查 `app/main.py` 中是否有新API：

```bash
grep -n "POST.*api/memorial/create" /opt/pet-memory-star/app/main.py
```

应该看到：
```
283:@app.post("/api/memorial/create")
```

如果没有，说明代码还没同步，再执行一次：

```bash
cd /opt/pet-memory-star
git fetch --all
git log origin/main -1 --oneline
git reset --hard origin/main
grep -n "POST.*api/memorial/create" app/main.py
```

## 完成后

在小程序中：
1. 重新编译
2. 测试创建纪念馆功能
3. 应该可以成功创建了 ✅

