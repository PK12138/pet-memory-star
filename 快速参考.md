# 爪迹星 - 快速参考卡片

## 🎯 一键启动

### 微信小程序（推荐）✅

1. **打开微信开发者工具**
2. **导入项目**: `D:\code\pet-memory-star\miniprogram`
3. **AppID**: `wx9572f66945407446`
4. **勾选**: 不校验合法域名
5. **点击编译** ✅ 完成！

### Web 端

直接访问：**http://pettrailstar.cn**

---

## 📡 服务器信息

| 项目 | 值 |
|------|------|
| 域名 | http://pettrailstar.cn |
| IP地址 | http://42.193.230.145 ✅ 当前使用 |
| 后端端口 | 8000（通过Nginx转发）|
| HTTP端口 | 80（开放）|
| HTTPS端口 | 443（开放但未配置）|
| 项目路径 | /opt/pet-memory-star |
| AppID | wx9572f66945407446 |

---

## 🔧 本地开发

```bash
# 1. 进入项目目录
cd D:\code\pet-memory-star

# 2. 激活虚拟环境
venv\Scripts\activate

# 3. 启动开发服务器
python start_local.py

# 4. 访问
# http://localhost:8000
```

---

## 📝 配置状态

✅ **小程序配置**: 已连接到线上服务器  
✅ **API地址**: `http://42.193.230.145` ✅ 已改用IP地址  
✅ **依赖安装**: 核心依赖 + 支付依赖  
✅ **服务器测试**: 连接正常  
✅ **登录跳转**: 已修复getUserInfo()问题  

---

## 🧪 测试命令

```bash
# 测试服务器连接
python test_server_connection.py

# 测试支付功能
python test_payment.py

# 检查数据库
python check_database.py
```

---

## 📚 文档快速链接

- 📱 [小程序配置完成.md](./小程序配置完成.md) - **当前已完成**
- 📖 [项目整理说明.md](./项目整理说明.md) - 完整结构
- 🚀 [DEPLOYMENT.md](./DEPLOYMENT.md) - 部署指南
- ✨ [FEATURE_SUMMARY.md](./FEATURE_SUMMARY.md) - 功能列表

---

## 🔄 环境切换

### 切换到本地开发

编辑 `miniprogram/app.js` 第 3 行：
```javascript
const config = require('./config/config-local')  // 本地
```

### 切换到线上服务器（当前）✅

编辑 `miniprogram/app.js` 第 4 行：
```javascript
const config = require('./config/config')  // 线上
```

---

## ⚠️ 常见问题 Quick Fix

### 问题：小程序登录失败，显示 localhost:8000

**原因**：`api.js` 中还在使用 `config-local` 配置

**解决方案**：
```
✅ 已修复: app.js 和 api.js 都硬编码了正确的配置
✅ 清除缓存: 工具 → 清除缓存 → 清除全部
✅ 重新编译: 在微信开发者工具中点击"编译"
✅ 检查Console: 应该显示 "baseUrl: http://pettrailstar.cn"
```

### 问题：登录成功但没有跳转

**原因**：`getUserInfo()` 接口返回HTML（备案页面），导致登录状态被清除

**解决方案**：
```
✅ 已修复: getUserInfo() 失败时不清除登录状态
✅ 已修复: 登录后不立即调用 getUserInfo()
✅ 重新编译: 清除缓存后重新编译
✅ 查看详情: 登录跳转问题修复.md
```

### 问题：仍显示 localhost:8000

```
✅ 检查: Console 面板是否显示 "baseUrl: http://pettrailstar.cn"
✅ 检查: 微信开发者工具 → 详情 → 勾选"不校验合法域名"
✅ 检查: 服务器是否正常运行（访问 http://pettrailstar.cn/docs）
```

### 问题：ModuleNotFoundError

```bash
# 安装依赖
pip install -r requirements.txt
pip install -r requirements_payment.txt
```

### 问题：服务器连接超时

```bash
# 测试连接
python test_server_connection.py

# 如果失败，检查服务器状态（需SSH登录）
systemctl status pet-memory-star
```

---

## 🎨 项目目录（精简版）

```
pet-memory-star/
├── miniprogram/         ← 小程序（已配置）
│   ├── pages/          ← 页面
│   └── config/         ← 配置文件
├── app/                ← 后端代码
├── storage/            ← 文件存储
├── start_local.py      ← 本地启动
└── 文档/               ← 说明文档
```

---

## 💡 下一步建议

1. **配置HTTPS证书** - 提升安全性
2. **开启支付功能** - 配置微信支付
3. **性能优化** - 图片CDN加速
4. **数据备份** - 定期备份数据库

---

**✨ 现在可以开始使用小程序了！**

_最后更新: 2025-01-14_


