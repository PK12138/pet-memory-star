# 🚀 快速部署脚本

## 方案1：在服务器上直接拉取最新代码（推荐）

### 步骤1：提交本地代码到Git
```bash
cd pet-memory-star
git add app/main.py
git commit -m "修复访问统计API：从Form改为JSON格式"
git push origin main
```

### 步骤2：在服务器上拉取代码
```bash
ssh root@42.193.230.145
cd /opt/pet-memory-star
git pull origin main
```

### 步骤3：重启服务
```bash
# 查找进程
ps aux | grep start_server.py

# 杀掉进程（替换为实际的PID）
kill <PID>

# 重新启动
nohup python3 start_server.py > server.log 2>&1 &

# 验证服务启动
tail -f server.log
```

---

## 方案2：直接在服务器上修改（临时方案）

```bash
ssh root@42.193.230.145
cd /opt/pet-memory-star

# 备份原文件
cp app/main.py app/main.py.backup_$(date +%Y%m%d_%H%M%S)

# 编辑文件
nano app/main.py
```

找到第786-803行（`@app.post("/api/visit-stat")`），修改为：

```python
@app.post("/api/visit-stat")
async def record_visit(
    visit_data: dict,
    request: Request = None
):
    """记录访问统计"""
    try:
        memorial_id = visit_data.get("memorial_id")
        if not memorial_id:
            return {"success": False, "error": "缺少memorial_id参数"}
        
        client_ip = request.client.host if request else "unknown"
        user_agent = request.headers.get("user-agent", "unknown") if request else "unknown"
        
        db.save_visit_stat(memorial_id, client_ip, user_agent)
        return {"success": True, "message": "访问记录成功"}
    except Exception as e:
        return {"success": False, "error": str(e)}
```

保存后重启服务：
```bash
# 杀掉进程
pkill -f start_server.py

# 重新启动
cd /opt/pet-memory-star
nohup python3 start_server.py > server.log 2>&1 &

# 验证
tail -f server.log
```

---

## 方案3：一键重启脚本

在服务器上创建重启脚本：
```bash
cat > /opt/pet-memory-star/restart.sh << 'EOF'
#!/bin/bash
echo "🔄 开始重启服务..."

# 切换到项目目录
cd /opt/pet-memory-star

# 杀掉旧进程
echo "停止旧进程..."
pkill -f start_server.py
sleep 2

# 启动新进程
echo "启动新进程..."
nohup python3 start_server.py > server.log 2>&1 &

# 等待启动
sleep 3

# 显示日志
echo "✅ 服务已重启，最新日志："
tail -n 20 server.log
EOF

chmod +x /opt/pet-memory-star/restart.sh
```

使用：
```bash
ssh root@42.193.230.145
/opt/pet-memory-star/restart.sh
```

---

## 验证部署成功

### 1. 检查服务状态
```bash
ps aux | grep start_server.py
```

### 2. 检查端口监听
```bash
ss -tulnp | grep :80
```

### 3. 测试API
```bash
curl -X POST http://42.193.230.145/api/visit-stat \
  -H "Content-Type: application/json" \
  -H "x-session-token: YOUR_TOKEN" \
  -d '{"memorial_id": "test123"}'
```

应该返回：
```json
{"success": true, "message": "访问记录成功"}
```

---

## 🔥 快速命令（推荐）

### 本地提交：
```bash
cd D:\code\pet-memory-star
git add app/main.py
git commit -m "修复访问统计API数据格式"
git push
```

### 服务器部署：
```bash
ssh root@42.193.230.145 "cd /opt/pet-memory-star && git pull && pkill -f start_server.py && nohup python3 start_server.py > server.log 2>&1 & sleep 3 && tail -n 20 server.log"
```

这一条命令完成：拉取代码 → 重启服务 → 显示日志

---

**修复完成后，重新在微信开发者工具中测试访问统计功能！**

