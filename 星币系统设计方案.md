# 🌟 星币系统完整设计方案

## 📋 系统概述

**星币（Star Coins）**是爪迹星的虚拟货币系统，用户通过完成各种任务获取星币，用星币解锁会员功能和高级服务。

### 核心理念
- 🎁 **免费获取** - 用户无需付费即可获得星币
- 🎮 **游戏化** - 通过任务、签到等方式提升活跃度
- 💰 **广告变现** - 激励视频广告是主要收入来源
- ⚖️ **平衡设计** - 免费用户也能享受核心功能

---

## 💰 星币经济系统

### 1. 星币获取途径

| 方式 | 奖励星币 | 限制 | 说明 |
|------|---------|------|------|
| 📅 每日签到 | +10 | 每天1次 | 连续签到有额外奖励 |
| 📸 上传照片 | +5 | 每天最多3次 | 上传高质量照片获得 |
| ✍️ 写纪念文字 | +20 | 每天最多2次 | 撰写100字以上 |
| 🎬 观看广告 | +50 | 每天最多5次 | 激励视频广告 |
| 👥 邀请好友 | +100 | 无限制 | 好友注册成功 |
| 🎯 完成新手任务 | +200 | 一次性 | 完成引导流程 |
| 🎁 首次登录 | +100 | 一次性 | 新用户福利 |
| 💬 AI对话 | +2 | 每天最多10次 | 每次对话获得 |
| 🌙 记录梦境 | +15 | 每天最多2次 | 记录梦境日记 |
| 📝 心情日记 | +15 | 每天最多2次 | 记录心情变化 |

### 2. 星币消费方式

| 项目 | 消费星币 | 说明 |
|------|---------|------|
| 🎖️ 月度会员 | 300 | 30天会员权益 |
| 👑 年度会员 | 3000 | 365天会员权益（送300） |
| 💎 终身会员 | 10000 | 永久会员（限时优惠） |
| 🤖 AI对话（高级） | 10/次 | 深度对话功能 |
| 🎨 专属主题 | 50-200 | 解锁特殊主题 |
| 💫 星星特效 | 30 | 特殊星空特效 |
| 🏛️ 纪念馆装饰 | 50-500 | 背景、音乐等 |

### 3. 每日最大收益

**免费用户每日可获得星币**：
```
签到：10
上传照片：5×3 = 15
写纪念文字：20×2 = 40
观看广告：50×5 = 250
AI对话：2×10 = 20
梦境日记：15×2 = 30
心情日记：15×2 = 30
─────────────────
总计：395星币/天
```

**月度会员获取时间**：
- 勤奋用户：~3-4天（每天300+星币）
- 普通用户：~7-10天（每天100星币）
- 休闲用户：~20-30天（每天30星币）

---

## 🗄️ 数据库设计

### 1. 用户星币表（user_coins）

```sql
CREATE TABLE IF NOT EXISTS user_coins (
    id INTEGER PRIMARY KEY AUTOINCREMENT,
    user_id INTEGER NOT NULL UNIQUE,
    balance INTEGER DEFAULT 0,              -- 当前余额
    total_earned INTEGER DEFAULT 0,         -- 累计获得
    total_spent INTEGER DEFAULT 0,          -- 累计消费
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    updated_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    FOREIGN KEY (user_id) REFERENCES users(id)
);
```

### 2. 星币交易记录表（coin_transactions）

```sql
CREATE TABLE IF NOT EXISTS coin_transactions (
    id INTEGER PRIMARY KEY AUTOINCREMENT,
    user_id INTEGER NOT NULL,
    amount INTEGER NOT NULL,                -- 星币数量（正数=获得，负数=消费）
    type TEXT NOT NULL,                     -- 类型：sign_in, watch_ad, spend等
    description TEXT,                       -- 描述：如"每日签到"
    metadata TEXT,                          -- JSON格式的额外信息
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    FOREIGN KEY (user_id) REFERENCES users(id)
);

CREATE INDEX idx_coin_transactions_user ON coin_transactions(user_id);
CREATE INDEX idx_coin_transactions_type ON coin_transactions(type);
```

### 3. 每日签到记录表（daily_sign_in）

```sql
CREATE TABLE IF NOT EXISTS daily_sign_in (
    id INTEGER PRIMARY KEY AUTOINCREMENT,
    user_id INTEGER NOT NULL,
    sign_date DATE NOT NULL,                -- 签到日期
    continuous_days INTEGER DEFAULT 1,      -- 连续签到天数
    reward_coins INTEGER NOT NULL,          -- 获得的星币
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    UNIQUE(user_id, sign_date),
    FOREIGN KEY (user_id) REFERENCES users(id)
);

CREATE INDEX idx_daily_sign_in_user ON daily_sign_in(user_id);
```

### 4. 任务完成记录表（task_completions）

```sql
CREATE TABLE IF NOT EXISTS task_completions (
    id INTEGER PRIMARY KEY AUTOINCREMENT,
    user_id INTEGER NOT NULL,
    task_type TEXT NOT NULL,                -- 任务类型：upload_photo, write_text等
    completion_date DATE NOT NULL,          -- 完成日期
    completion_count INTEGER DEFAULT 1,     -- 当天完成次数
    reward_coins INTEGER NOT NULL,          -- 获得的星币
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    FOREIGN KEY (user_id) REFERENCES users(id)
);

CREATE INDEX idx_task_completions_user ON task_completions(user_id);
CREATE INDEX idx_task_completions_date ON task_completions(completion_date);
```

### 5. 激励广告观看记录表（ad_views）

```sql
CREATE TABLE IF NOT EXISTS ad_views (
    id INTEGER PRIMARY KEY AUTOINCREMENT,
    user_id INTEGER NOT NULL,
    ad_unit_id TEXT NOT NULL,               -- 广告位ID
    view_date DATE NOT NULL,                -- 观看日期
    view_count INTEGER DEFAULT 1,           -- 当天观看次数
    reward_coins INTEGER NOT NULL,          -- 获得的星币
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    FOREIGN KEY (user_id) REFERENCES users(id)
);

CREATE INDEX idx_ad_views_user ON ad_views(user_id);
```

---

## 🔧 后端API设计

### 1. 星币余额相关

#### GET `/api/coins/balance`
获取用户星币余额

**响应**：
```json
{
  "success": true,
  "balance": 520,
  "total_earned": 1000,
  "total_spent": 480
}
```

#### GET `/api/coins/transactions`
获取交易记录

**参数**：
- `page`: 页码（默认1）
- `limit`: 每页数量（默认20）
- `type`: 交易类型（可选）

**响应**：
```json
{
  "success": true,
  "transactions": [
    {
      "id": 123,
      "amount": 50,
      "type": "watch_ad",
      "description": "观看激励视频广告",
      "created_at": "2025-11-01 10:30:00"
    }
  ],
  "total": 150,
  "page": 1,
  "total_pages": 8
}
```

### 2. 每日签到

#### POST `/api/coins/sign-in`
每日签到

**响应**：
```json
{
  "success": true,
  "reward": 10,
  "continuous_days": 5,
  "message": "签到成功！连续签到5天，获得10星币",
  "new_balance": 530
}
```

**连续签到奖励**：
- 1-6天：10星币/天
- 第7天：50星币（周奖励）
- 第14天：100星币
- 第30天：300星币（月度大奖）

### 3. 任务完成

#### POST `/api/coins/task/complete`
完成任务获得星币

**请求**：
```json
{
  "task_type": "upload_photo",
  "metadata": {
    "memorial_id": 123,
    "photo_count": 1
  }
}
```

**响应**：
```json
{
  "success": true,
  "reward": 5,
  "message": "上传照片成功，获得5星币",
  "today_count": 2,
  "max_count": 3,
  "new_balance": 535
}
```

### 4. 观看广告

#### POST `/api/coins/watch-ad`
观看激励视频广告

**请求**：
```json
{
  "ad_unit_id": "adunit-xxxxxx"
}
```

**响应**：
```json
{
  "success": true,
  "reward": 50,
  "message": "观看广告成功，获得50星币",
  "today_count": 3,
  "max_count": 5,
  "new_balance": 585
}
```

### 5. 消费星币

#### POST `/api/coins/spend`
消费星币

**请求**：
```json
{
  "type": "monthly_vip",
  "amount": 300,
  "description": "购买月度会员"
}
```

**响应**：
```json
{
  "success": true,
  "spent": 300,
  "new_balance": 285,
  "message": "兑换成功！已开通月度会员"
}
```

---

## 📱 前端界面设计

### 1. 星币中心页面（coins-center）

**功能区域**：
1. **星币余额展示**
   - 当前余额（大号显示）
   - 累计获得、累计消费
   - 星币动画效果

2. **每日任务卡片**
   - 签到状态和连续天数
   - 今日可完成任务列表
   - 进度条显示

3. **快速获取入口**
   - 观看广告（最醒目）
   - 邀请好友
   - 完成任务

4. **交易记录**
   - 最近10条记录
   - 查看更多按钮

### 2. 星币商城页面（coins-shop）

**商品分类**：
1. **会员权益**
   - 月度会员卡片
   - 年度会员卡片
   - 终身会员卡片

2. **个性化装饰**
   - 主题皮肤
   - 星空特效
   - 纪念馆装饰

3. **功能解锁**
   - AI高级对话
   - 数据导出
   - 专属功能

### 3. 用户中心集成

在现有用户中心添加：
- 星币余额显示（显眼位置）
- 快速签到按钮
- 今日任务提醒

---

## 🎬 微信激励视频广告接入

### 1. 申请广告位

登录[微信小程序后台](https://mp.weixin.qq.com/)：
1. 进入「流量主」→「广告管理」
2. 创建激励视频广告位
3. 获取广告位ID（adunit-xxxxxx）

**要求**：
- 小程序已发布
- 累计独立访问用户≥1000
- 无违规记录

### 2. 前端集成

```javascript
// pages/coins-center/coins-center.js
Page({
  data: {
    rewardedVideoAd: null,
    todayAdCount: 0,
    maxAdCount: 5
  },

  onLoad() {
    // 创建激励视频广告实例
    if (wx.createRewardedVideoAd) {
      this.data.rewardedVideoAd = wx.createRewardedVideoAd({
        adUnitId: 'adunit-xxxxxx' // 替换为真实广告位ID
      })

      // 监听广告关闭事件
      this.data.rewardedVideoAd.onClose((res) => {
        if (res && res.isEnded) {
          // 用户看完广告，发放奖励
          this.giveAdReward()
        } else {
          wx.showToast({
            title: '未看完广告，无法获得奖励',
            icon: 'none'
          })
        }
      })

      // 监听广告加载失败
      this.data.rewardedVideoAd.onError((err) => {
        console.error('广告加载失败', err)
        wx.showToast({
          title: '广告加载失败，请稍后再试',
          icon: 'none'
        })
      })
    }
  },

  // 观看广告按钮
  watchAd() {
    if (this.data.todayAdCount >= this.data.maxAdCount) {
      wx.showToast({
        title: '今日观看次数已用完',
        icon: 'none'
      })
      return
    }

    // 显示广告
    this.data.rewardedVideoAd.show()
      .catch(() => {
        // 广告未加载完成，先加载再显示
        this.data.rewardedVideoAd.load()
          .then(() => this.data.rewardedVideoAd.show())
          .catch((err) => {
            console.error('广告显示失败', err)
          })
      })
  },

  // 发放广告奖励
  async giveAdReward() {
    const app = getApp()
    try {
      const res = await app.request({
        url: '/api/coins/watch-ad',
        method: 'POST',
        data: {
          ad_unit_id: 'adunit-xxxxxx'
        }
      })

      if (res.success) {
        wx.showToast({
          title: `获得${res.reward}星币！`,
          icon: 'success'
        })
        
        this.setData({
          todayAdCount: res.today_count
        })
        
        // 刷新余额
        this.loadBalance()
      }
    } catch (error) {
      console.error('奖励发放失败', error)
    }
  }
})
```

### 3. 广告收益预估

**收益计算**：
- CPM（千次展示收入）：20-100元
- 用户观看1次广告 ≈ 0.02-0.1元
- 1000用户/天，每人看3次 = 60-300元/天

**成本对比**：
- 用户看1次广告获得50星币
- 300星币=月度会员
- 用户成本：6次广告×0.05元 = 0.3元
- 传统付费：29.9元

**结论**：通过广告变现，用户免费使用，平台仍有收益！

---

## 🎮 游戏化设计

### 1. 成就系统

| 成就 | 条件 | 奖励 |
|------|------|------|
| 🌱 初来乍到 | 注册成功 | 100星币 |
| 🔥 签到达人 | 连续签到7天 | 50星币 |
| 💪 签到狂魔 | 连续签到30天 | 300星币 |
| 📸 摄影师 | 上传100张照片 | 200星币 |
| ✍️ 作家 | 写50篇日记 | 200星币 |
| 👥 社交达人 | 邀请10个好友 | 500星币 |
| 💎 土豪 | 累计获得10000星币 | 专属徽章 |

### 2. 等级系统

根据累计获得星币升级：
- LV1：新手（0-500）
- LV2：熟练（501-2000）
- LV3：专家（2001-5000）
- LV4：大师（5001-10000）
- LV5：传奇（10001+）

每升级奖励50-200星币

### 3. 限时活动

- 周末双倍星币
- 节日特别任务
- 签到挑战赛
- 邀请竞赛

---

## 🛡️ 防刷机制

### 1. 每日限制
- 所有任务都有每日次数上限
- IP限制（同IP多账号检测）
- 设备限制（同设备多账号检测）

### 2. 行为检测
- 异常快速操作检测
- 广告观看时长验证
- 任务完成真实性验证

### 3. 风控策略
```python
# 后端风控逻辑示例
def check_anti_fraud(user_id, task_type):
    # 1. 检查今日次数
    today_count = get_today_task_count(user_id, task_type)
    if today_count >= MAX_COUNT[task_type]:
        return False, "今日次数已达上限"
    
    # 2. 检查操作频率
    last_time = get_last_task_time(user_id, task_type)
    if time.time() - last_time < 60:  # 1分钟内不能重复
        return False, "操作过于频繁"
    
    # 3. 检查IP/设备
    if is_suspicious_device(user_id):
        return False, "检测到异常行为"
    
    return True, "验证通过"
```

---

## 📊 数据分析指标

需要追踪的关键指标：
1. **DAU/MAU** - 日活/月活用户
2. **签到率** - 每日签到用户比例
3. **广告观看率** - 广告观看/DAU
4. **任务完成率** - 各任务完成情况
5. **星币消费情况** - 哪些商品最受欢迎
6. **广告收益** - 每日/每月广告收入
7. **用户留存** - 次日/7日/30日留存

---

## 🚀 实施计划

### 第1阶段（1-2天）：基础框架
- ✅ 创建数据库表
- ✅ 实现后端API
- ✅ 基础前端页面

### 第2阶段（2-3天）：核心功能
- ✅ 签到系统
- ✅ 任务系统
- ✅ 星币商城

### 第3阶段（1-2天）：广告集成
- ✅ 申请广告位
- ✅ 接入激励视频
- ✅ 测试奖励发放

### 第4阶段（持续）：优化迭代
- ✅ 数据分析
- ✅ 用户反馈
- ✅ 功能优化

---

## 💡 成功关键要素

### 1. 平衡性
- 免费用户也能获得核心功能
- 付费/广告路径都有价值
- 不能让系统失去平衡

### 2. 用户体验
- 获得星币要简单有趣
- 不能太繁琐
- 即时反馈很重要

### 3. 持续运营
- 定期更新任务
- 举办活动
- 保持新鲜感

---

**准备好开始实现了吗？我会从数据库开始，逐步实现完整系统！** 🚀


