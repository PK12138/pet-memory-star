# 🔥 紧急修复：服务器端口无法访问

## 🎯 问题确认

**测试结果**：
- ✅ 服务器在线（Ping成功）
- ❌ 80端口不通
- ❌ 8000端口不通

**结论**：服务器防火墙或安全组阻止了外部访问。

---

## 🚀 立即执行（SSH登录服务器）

### 1. SSH登录
```bash
ssh root@42.193.230.145
```

### 2. 一键修复脚本

复制以下命令，粘贴到服务器终端执行：

```bash
#!/bin/bash

echo "================================"
echo "🔧 开始配置服务器端口访问"
echo "================================"

# 1. 检查服务运行状态
echo ""
echo "📊 1. 检查服务运行状态..."
if pgrep -f uvicorn > /dev/null; then
    echo "✅ 服务正在运行"
    ps aux | grep uvicorn | grep -v grep
else
    echo "❌ 服务未运行，准备启动..."
    cd /opt/pet-memory-star
    source venv/bin/activate
    nohup python start_server.py > server.log 2>&1 &
    sleep 3
    echo "✅ 服务已启动"
fi

# 2. 检查端口监听
echo ""
echo "📊 2. 检查端口监听..."
netstat -tulpn | grep 8000 || ss -tulpn | grep 8000

# 3. 开放防火墙端口
echo ""
echo "🔥 3. 配置防火墙..."

# 检查防火墙类型
if command -v firewall-cmd &> /dev/null; then
    echo "检测到 firewalld..."
    
    # 开放8000端口
    sudo firewall-cmd --permanent --add-port=8000/tcp
    
    # 同时开放80端口（为将来配置Nginx准备）
    sudo firewall-cmd --permanent --add-port=80/tcp
    
    # 重载防火墙
    sudo firewall-cmd --reload
    
    echo "✅ 防火墙规则已添加"
    echo ""
    echo "当前开放的端口："
    sudo firewall-cmd --list-ports
    
elif command -v ufw &> /dev/null; then
    echo "检测到 UFW..."
    sudo ufw allow 8000/tcp
    sudo ufw allow 80/tcp
    sudo ufw reload
    echo "✅ UFW规则已添加"
    
elif command -v iptables &> /dev/null; then
    echo "检测到 iptables..."
    sudo iptables -I INPUT -p tcp --dport 8000 -j ACCEPT
    sudo iptables -I INPUT -p tcp --dport 80 -j ACCEPT
    sudo service iptables save
    echo "✅ iptables规则已添加"
    
else
    echo "⚠️ 未检测到防火墙，可能已经开放"
fi

# 4. 测试本地连接
echo ""
echo "🧪 4. 测试本地连接..."
curl -s http://localhost:8000/api/health | head -20

echo ""
echo "================================"
echo "✅ 配置完成！"
echo "================================"
echo ""
echo "📋 接下来的操作："
echo "1. 在云服务器控制台开放安全组端口 8000"
echo "2. 重新编译小程序"
echo "3. 测试连接"
echo ""
echo "🔍 查看服务日志："
echo "tail -50 /opt/pet-memory-star/server.log"
echo ""
```

---

## ☁️ 云服务器安全组配置（必须！）

### 腾讯云 OpenCloudOS

#### 方法1：使用控制台
1. **登录腾讯云控制台**：https://console.cloud.tencent.com/cvm
2. **找到你的服务器** (42.193.230.145)
3. 点击实例ID → **安全组**
4. 点击安全组ID → **修改规则**
5. **入站规则** → **添加规则**
6. 配置：
   ```
   类型: 自定义
   来源: 0.0.0.0/0
   协议端口: TCP:8000
   策略: 允许
   备注: 宠物纪念馆API端口
   ```
7. 点击**完成**

#### 方法2：使用腾讯云CLI（如果已安装）
```bash
# 查看安全组ID
tccli cvm DescribeInstances --InstanceIds '["你的实例ID"]'

# 添加规则（替换sg-xxx为你的安全组ID）
tccli vpc CreateSecurityGroupPolicies \
  --SecurityGroupId sg-xxx \
  --SecurityGroupPolicySet '{
    "Ingress": [{
      "Protocol": "TCP",
      "Port": "8000",
      "CidrBlock": "0.0.0.0/0",
      "Action": "ACCEPT"
    }]
  }'
```

### 阿里云

1. 登录阿里云控制台：https://ecs.console.aliyun.com
2. 实例 → 找到服务器 → **更多** → **网络和安全组** → **安全组配置**
3. 配置规则 → **添加安全组规则**
4. 配置：
   ```
   授权策略: 允许
   协议类型: 自定义TCP
   端口范围: 8000/8000
   授权对象: 0.0.0.0/0
   描述: 宠物纪念馆API
   ```
5. 确定

### 华为云

1. 登录华为云控制台
2. 弹性云服务器 → 安全组
3. 配置规则 → 入方向规则 → **添加规则**
4. 配置：
   ```
   协议: TCP
   端口: 8000
   源地址: 0.0.0.0/0
   ```

---

## 🧪 验证配置

### 在服务器上测试
```bash
# 1. 测试本地连接
curl http://localhost:8000/api/health

# 应该返回：
# {"status":"healthy","message":"服务器运行正常",...}

# 2. 测试外网连接（从服务器本身）
curl http://42.193.230.145:8000/api/health
```

### 在本地测试
```powershell
# Windows PowerShell
Test-NetConnection -ComputerName 42.193.230.145 -Port 8000

# 应该显示：
# TcpTestSucceeded : True  ✅
```

或使用浏览器访问：
```
http://42.193.230.145:8000/api/health
```

---

## 📊 故障排查

### 问题1：服务未运行

**检查**：
```bash
ps aux | grep uvicorn
```

**解决**：
```bash
cd /opt/pet-memory-star
source venv/bin/activate
nohup python start_server.py > server.log 2>&1 &
```

### 问题2：端口未监听

**检查**：
```bash
netstat -tulpn | grep 8000
# 或
ss -tulpn | grep 8000
```

**应该看到**：
```
tcp  0  0  0.0.0.0:8000  0.0.0.0:*  LISTEN  12345/python
```

**如果没有**，检查日志：
```bash
tail -100 /opt/pet-memory-star/server.log
```

### 问题3：防火墙仍然阻止

**检查规则**：
```bash
# firewalld
sudo firewall-cmd --list-all

# ufw
sudo ufw status

# iptables
sudo iptables -L -n | grep 8000
```

**临时关闭防火墙测试**（不推荐生产环境）：
```bash
# firewalld
sudo systemctl stop firewalld

# ufw
sudo ufw disable
```

**测试后记得重新开启**：
```bash
# firewalld
sudo systemctl start firewalld

# ufw
sudo ufw enable
```

### 问题4：安全组未生效

- 确认安全组已绑定到实例
- 确认规则优先级
- 等待1-2分钟让规则生效
- 在云控制台查看安全组事件日志

---

## 🎯 完整检查清单

在服务器上执行：

```bash
echo "=== 完整系统检查 ==="
echo ""

echo "1. 服务进程："
ps aux | grep uvicorn | grep -v grep

echo ""
echo "2. 端口监听："
ss -tulpn | grep 8000

echo ""
echo "3. 防火墙状态："
sudo firewall-cmd --state 2>/dev/null || echo "firewalld未运行"
sudo ufw status 2>/dev/null || echo "ufw未运行"

echo ""
echo "4. 防火墙规则："
sudo firewall-cmd --list-ports 2>/dev/null || \
sudo ufw status numbered 2>/dev/null || \
sudo iptables -L INPUT -n | grep 8000

echo ""
echo "5. 本地连接测试："
curl -I http://localhost:8000/api/health 2>&1 | head -5

echo ""
echo "6. 最新日志："
tail -20 /opt/pet-memory-star/server.log

echo ""
echo "=== 检查完成 ==="
```

---

## 💡 推荐配置（长期方案）

### 配置Nginx反向代理

这样可以使用标准的80端口，小程序配置更简洁：

```bash
# 1. 安装Nginx
sudo yum install -y nginx  # CentOS/OpenCloudOS
# 或
sudo apt install -y nginx  # Ubuntu/Debian

# 2. 配置Nginx
sudo tee /etc/nginx/conf.d/petmemory.conf > /dev/null << 'EOF'
server {
    listen 80;
    server_name 42.193.230.145 pettrailstar.cn;
    
    client_max_body_size 100M;
    
    location / {
        proxy_pass http://127.0.0.1:8000;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        
        proxy_connect_timeout 600;
        proxy_send_timeout 600;
        proxy_read_timeout 600;
    }
    
    location /storage/ {
        alias /opt/pet-memory-star/storage/;
        expires 30d;
    }
}
EOF

# 3. 测试配置
sudo nginx -t

# 4. 启动Nginx
sudo systemctl start nginx
sudo systemctl enable nginx

# 5. 开放80端口
sudo firewall-cmd --permanent --add-service=http
sudo firewall-cmd --reload
```

**配置Nginx后，小程序可以使用**：
```javascript
baseUrl: 'http://42.193.230.145'  // 不需要端口号
```

---

## 📞 快速帮助

如果仍有问题：

1. **查看服务器日志**：
   ```bash
   tail -100 /opt/pet-memory-star/server.log
   ```

2. **检查系统日志**：
   ```bash
   sudo journalctl -xe | tail -50
   ```

3. **查看Nginx日志**（如果使用）：
   ```bash
   sudo tail -50 /var/log/nginx/error.log
   ```

---

**✨ 按照以上步骤配置后，小程序应该可以正常连接服务器！**

_创建时间: 2025-01-14_

