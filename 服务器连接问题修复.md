# 🔧 服务器连接问题修复

## 🎯 问题诊断

### 错误现象
小程序Console显示：
```
❌ POST http://42.193.230.145/api/memorial/create
   net::ERR_CONNECTION_TIMED_OUT

❌ {errMsg: "request:fail_timeout", errno: undefined}
```

### 根本原因

**服务器80端口无法访问**

测试结果：
```powershell
Test-NetConnection -ComputerName 42.193.230.145 -Port 80

TcpTestSucceeded : False  ❌ 80端口不通
PingSucceeded    : True   ✅ 服务器在线
```

**可能的原因**：
1. 防火墙阻止80端口
2. 服务器实际监听8000端口，没有配置Nginx转发
3. 云服务器安全组未开放80端口

---

## 🔧 快速修复（临时方案）

### 修改小程序配置，使用8000端口直连

#### 1. 修改 `app.js`

```javascript
// 修改前
baseUrl: 'http://42.193.230.145',

// 修改后
baseUrl: 'http://42.193.230.145:8000',
```

#### 2. 修改 `utils/api.js`

```javascript
// 修改前
const config = {
  baseUrl: 'http://42.193.230.145'
}

// 修改后
const config = {
  baseUrl: 'http://42.193.230.145:8000'
}
```

---

## ✅ 已修改的文件

- ✅ `miniprogram/app.js` - 第9行
- ✅ `miniprogram/utils/api.js` - 第7行

---

## 🧪 测试步骤

### 1. 重新编译小程序
在微信开发者工具中：
1. 点击"编译"按钮
2. 或按 `Ctrl + B`

### 2. 清除缓存（重要！）
1. 工具 → 清除缓存 → 清除全部
2. 重新编译

### 3. 测试登录和创建

登录后，进入性格测试，完成测试并创建纪念馆。

**Console应该显示**：
```
✅ POST http://42.193.230.145:8000/api/memorial/create 200 OK
```

---

## 🎯 长期解决方案

### 方案1：配置Nginx反向代理（推荐）

在服务器上配置Nginx，将80端口请求转发到8000端口。

#### 安装Nginx
```bash
# CentOS/OpenCloudOS
sudo yum install -y nginx

# Ubuntu/Debian
sudo apt install -y nginx
```

#### 配置Nginx
```bash
# 创建配置文件
sudo vi /etc/nginx/conf.d/petmemory.conf
```

添加以下内容：
```nginx
server {
    listen 80;
    server_name 42.193.230.145 pettrailstar.cn;
    
    # 允许大文件上传
    client_max_body_size 100M;
    
    # API 代理
    location / {
        proxy_pass http://127.0.0.1:8000;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        
        # WebSocket 支持
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection "upgrade";
        
        # 超时设置
        proxy_connect_timeout 600;
        proxy_send_timeout 600;
        proxy_read_timeout 600;
    }
    
    # 静态文件
    location /storage/ {
        alias /opt/pet-memory-star/storage/;
        expires 30d;
        access_log off;
    }
}
```

#### 启动Nginx
```bash
# 测试配置
sudo nginx -t

# 启动Nginx
sudo systemctl start nginx
sudo systemctl enable nginx

# 查看状态
sudo systemctl status nginx
```

#### 开放防火墙
```bash
# CentOS/OpenCloudOS
sudo firewall-cmd --permanent --add-service=http
sudo firewall-cmd --reload

# Ubuntu/Debian (UFW)
sudo ufw allow 80/tcp
sudo ufw reload
```

#### 修改小程序配置
配置Nginx后，小程序可以使用：
```javascript
baseUrl: 'http://42.193.230.145'  // 不带端口号
```

---

### 方案2：开放8000端口（不推荐）

如果不想配置Nginx，可以开放8000端口：

#### 防火墙
```bash
# CentOS/OpenCloudOS
sudo firewall-cmd --permanent --add-port=8000/tcp
sudo firewall-cmd --reload

# Ubuntu/Debian (UFW)
sudo ufw allow 8000/tcp
sudo ufw reload
```

#### 云服务器安全组
在云服务器控制台：
1. 进入安全组配置
2. 添加入站规则：TCP 8000
3. 来源：0.0.0.0/0

**缺点**：
- 暴露了非标准端口
- 小程序需要带端口号访问
- 不够专业

---

## 📊 端口访问测试

### 测试80端口（当前不通）
```bash
Test-NetConnection -ComputerName 42.193.230.145 -Port 80
# TcpTestSucceeded : False ❌
```

### 测试8000端口
```bash
Test-NetConnection -ComputerName 42.193.230.145 -Port 8000
# 需要先开放防火墙和安全组
```

### 测试Nginx配置后
```bash
curl http://42.193.230.145/api/health
# 应该返回：{"status":"healthy",...}
```

---

## 🚨 注意事项

### 1. 小程序服务器域名配置

如果使用正式环境，需要在微信公众平台配置服务器域名：

1. 登录：https://mp.weixin.qq.com
2. 开发 → 开发管理 → 服务器域名
3. 添加：
   - `http://42.193.230.145`（需要备案）
   - `http://pettrailstar.cn`（需要备案）

### 2. HTTPS升级

生产环境建议使用HTTPS：

#### 申请SSL证书
```bash
# 使用Let's Encrypt免费证书
sudo yum install -y certbot python3-certbot-nginx
sudo certbot --nginx -d pettrailstar.cn
```

#### 小程序配置
```javascript
baseUrl: 'https://pettrailstar.cn'
```

### 3. 域名备案

如果使用域名 `pettrailstar.cn`，需要：
1. 在云服务商完成ICP备案
2. 备案通过后才能正常访问

---

## ✅ 当前配置（临时方案）

```javascript
// miniprogram/app.js
baseUrl: 'http://42.193.230.145:8000'

// miniprogram/utils/api.js
baseUrl: 'http://42.193.230.145:8000'
```

**优点**：
- ✅ 立即可用
- ✅ 无需服务器配置

**缺点**：
- ❌ 需要暴露8000端口
- ❌ URL不够简洁
- ❌ 不适合生产环境

---

## 🎯 推荐步骤

### 立即测试（使用8000端口）
1. ✅ 已修改小程序配置
2. 重新编译小程序
3. 测试登录和创建功能

### 后续优化（配置Nginx）
1. 在服务器安装Nginx
2. 配置反向代理
3. 开放80端口
4. 修改小程序配置回 `http://42.193.230.145`
5. 申请SSL证书升级HTTPS

---

**✨ 配置已修改！重新编译小程序，现在应该可以正常连接服务器了。**

_修复时间: 2025-01-14_

