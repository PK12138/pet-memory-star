# 🔍 服务器状态检查

## 当前问题

小程序显示 `net::ERR_CONNECTION_TIMED_OUT`，说明无法连接到服务器。

## 立即执行的检查命令

### 1. SSH 连接到服务器后，执行以下命令：

```bash
# 检查服务器进程
echo "=== 检查Python进程 ==="
ps aux | grep python3 | grep -v grep

# 检查端口监听
echo ""
echo "=== 检查端口80监听 ==="
ss -tulpn | grep :80

# 检查服务日志
echo ""
echo "=== 查看最近的服务日志 ==="
tail -100 /opt/pet-memory-star/nohup.out

# 检查服务器防火墙
echo ""
echo "=== 检查防火墙状态 ==="
firewall-cmd --list-all 2>/dev/null || ufw status 2>/dev/null || iptables -L -n | grep 80

# 测试本地连接
echo ""
echo "=== 测试本地API ==="
curl -I http://localhost/api/health
```

### 2. 如果服务没有运行，启动服务：

```bash
cd /opt/pet-memory-star
git fetch --all
git reset --hard origin/main
source venv/bin/activate
nohup python3 start_server.py > nohup.out 2>&1 &
sleep 3
tail -50 nohup.out
```

### 3. 如果服务运行中但无法访问，检查防火墙：

```bash
# 对于 firewalld (CentOS/RHEL)
sudo firewall-cmd --zone=public --add-port=80/tcp --permanent
sudo firewall-cmd --reload

# 对于 ufw (Ubuntu/Debian)
sudo ufw allow 80/tcp
sudo ufw reload
```

### 4. 从本地测试服务器连接：

在本地 PowerShell 中执行：

```powershell
# 测试连接
Test-NetConnection -ComputerName 42.193.230.145 -Port 80

# 测试 HTTP 请求
curl http://42.193.230.145/api/health
```

## 常见问题

### 问题 1：服务器进程不存在
**解决方案**：启动服务（见上方命令）

### 问题 2：端口未监听
**解决方案**：检查 `nohup.out` 日志，查看启动错误

### 问题 3：防火墙阻止
**解决方案**：开放端口 80（见上方命令）

### 问题 4：云服务器安全组未开放
**解决方案**：登录云服务器控制台，在安全组中添加规则：
- 端口：80
- 协议：TCP
- 源地址：0.0.0.0/0

## 快速修复脚本

将以下内容保存为 `/opt/pet-memory-star/restart.sh`：

```bash
#!/bin/bash
cd /opt/pet-memory-star
git fetch --all
git reset --hard origin/main
pkill -f "python3 start_server.py"
sleep 2
source venv/bin/activate
nohup python3 start_server.py > nohup.out 2>&1 &
sleep 3
echo "服务已启动，检查状态："
ps aux | grep "python3 start_server.py" | grep -v grep
ss -tulpn | grep :80
tail -20 nohup.out
```

然后执行：
```bash
chmod +x /opt/pet-memory-star/restart.sh
bash /opt/pet-memory-star/restart.sh
```

