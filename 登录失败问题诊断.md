# 🔧 登录失败问题诊断与解决

## 📋 问题描述

小程序登录时显示：
```
POST http://42.193.230.145/api/auth/login
net::ERR_CONNECTION_TIMED_OUT
```

**错误含义**：无法连接到服务器，连接超时。

---

## 🔍 问题诊断

### 症状分析

从控制台错误可以看到：
1. ❌ `ERR_CONNECTION_TIMED_OUT` - 连接超时
2. ❌ 请求 `http://42.193.230.145/api/auth/login` 失败
3. ❌ 重试多次仍然失败

### 可能原因

1. **服务器未运行** ⭐ 最可能
   - 服务进程已停止
   - 服务崩溃
   - 服务器重启后未自动启动

2. **端口问题**
   - 80端口未监听
   - 防火墙阻止

3. **网络问题**
   - 服务器网络故障
   - 云安全组配置变更

---

## ✅ 解决方案

### 方案1：检查并启动服务器（推荐）

#### 步骤1：SSH连接到服务器

```bash
ssh root@42.193.230.145
```

#### 步骤2：检查服务状态

```bash
# 检查uvicorn进程
ps aux | grep uvicorn

# 检查80端口监听
ss -tulpn | grep :80

# 查看服务日志
tail -50 /opt/pet-memory-star/server.log
```

#### 步骤3：启动服务

**如果服务未运行**，执行：

```bash
cd /opt/pet-memory-star

# 激活虚拟环境
source venv/bin/activate

# 停止所有旧进程（如果有）
pkill -f uvicorn

# 启动服务
nohup python3 start_server.py > server.log 2>&1 &

# 等待5秒
sleep 5

# 验证启动
curl http://localhost:80/api/health

# 查看日志
tail -20 server.log
```

**期望输出**：
```json
{"status":"healthy","message":"服务器运行正常","timestamp":"..."}
```

#### 步骤4：验证外网访问

在本地电脑测试：
```bash
curl http://42.193.230.145/api/health
```

---

### 方案2：使用本地服务器测试（临时）

如果服务器暂时无法访问，可以使用本地服务器测试小程序功能。

#### 步骤1：启动本地服务器

在本地项目目录：
```bash
cd D:\code\pet-memory-star

# 激活虚拟环境
.\venv\Scripts\activate

# 启动本地服务器
python start_local.py
```

#### 步骤2：修改小程序配置

修改 `miniprogram/app.js`：
```javascript
// 临时使用本地服务器
baseUrl: 'http://localhost:8000'
```

修改 `miniprogram/utils/api.js`：
```javascript
const config = {
  baseUrl: 'http://localhost:8000'
}
```

#### 步骤3：重新编译小程序

1. 在微信开发者工具中
2. 点击"编译"
3. 测试登录功能

#### ⚠️ 注意

使用本地服务器时：
- ✅ 可以测试小程序功能
- ❌ 无法测试真实网络环境
- ⚠️ 测试完成后记得改回服务器地址

---

### 方案3：检查云安全组配置

#### 步骤1：登录云服务器管理控制台

访问腾讯云/阿里云/其他云平台控制台

#### 步骤2：检查安全组规则

确认以下规则存在：

| 协议 | 端口 | 来源 | 策略 |
|------|------|------|------|
| TCP | 80 | 0.0.0.0/0 | 允许 |
| TCP | 443 | 0.0.0.0/0 | 允许 |
| TCP | 22 | 0.0.0.0/0 | 允许 |

#### 步骤3：检查服务器防火墙

```bash
# 检查防火墙状态（CentOS/RHEL）
systemctl status firewalld

# 或者（Ubuntu/Debian）
ufw status

# 如果启用了防火墙，添加规则
firewall-cmd --zone=public --add-port=80/tcp --permanent
firewall-cmd --reload

# 或者
ufw allow 80/tcp
```

---

## 🔧 快速修复脚本

创建 `fix_server.sh` 并在服务器上执行：

```bash
#!/bin/bash
# 快速修复服务器脚本

echo "🔍 检查服务状态..."
ps aux | grep uvicorn | grep -v grep

echo ""
echo "🔍 检查端口监听..."
ss -tulpn | grep :80

echo ""
echo "🔄 停止旧服务..."
pkill -f uvicorn

echo ""
echo "🚀 启动新服务..."
cd /opt/pet-memory-star
source venv/bin/activate
nohup python3 start_server.py > server.log 2>&1 &

echo ""
echo "⏳ 等待5秒..."
sleep 5

echo ""
echo "✅ 验证服务..."
curl http://localhost:80/api/health

echo ""
echo "📋 查看日志..."
tail -20 server.log

echo ""
echo "✅ 完成！"
```

**使用方法**：
```bash
# 上传脚本到服务器
scp fix_server.sh root@42.193.230.145:/opt/

# 在服务器上执行
ssh root@42.193.230.145
cd /opt
chmod +x fix_server.sh
./fix_server.sh
```

---

## 📊 故障排查流程图

```
登录失败
    ↓
能否SSH到服务器？
    ├─ NO → 检查服务器状态/联系云服务商
    └─ YES → 继续
         ↓
    服务是否运行？(ps aux | grep uvicorn)
    ├─ NO → 启动服务 (方案1步骤3)
    └─ YES → 继续
         ↓
    80端口是否监听？(ss -tulpn | grep :80)
    ├─ NO → 检查start_server.py配置
    └─ YES → 继续
         ↓
    本地能访问吗？(curl localhost:80/api/health)
    ├─ NO → 查看日志，检查代码
    └─ YES → 继续
         ↓
    外网能访问吗？(curl 42.193.230.145/api/health)
    ├─ NO → 检查防火墙/安全组
    └─ YES → 问题在小程序端
         ↓
    清除小程序缓存，重新编译
```

---

## 🎯 常见错误及解决

### 错误1：ERR_CONNECTION_TIMED_OUT

**原因**：服务器未响应
**解决**：
1. 检查服务是否运行
2. 检查防火墙/安全组
3. 检查网络连接

### 错误2：ERR_CONNECTION_REFUSED

**原因**：端口未监听
**解决**：
1. 检查服务是否启动
2. 检查端口配置（应该是80）
3. 检查服务日志

### 错误3：404 Not Found

**原因**：API路径错误
**解决**：
1. 检查URL路径：`/api/auth/login`
2. 检查后端路由配置
3. 查看服务器日志

### 错误4：500 Internal Server Error

**原因**：服务器内部错误
**解决**：
1. 查看服务器日志
2. 检查数据库连接
3. 检查环境变量

---

## 📝 验证清单

服务器修复后，逐项验证：

- [ ] SSH可以连接到服务器
- [ ] uvicorn进程正在运行
- [ ] 80端口正在监听
- [ ] 本地curl可以访问健康检查
- [ ] 外网curl可以访问健康检查
- [ ] 小程序可以访问登录接口
- [ ] 登录功能正常

---

## 💡 预防措施

### 1. 配置进程守护

使用systemd确保服务自动启动：

```bash
# 创建服务文件
sudo nano /etc/systemd/system/pet-memory-star.service
```

内容：
```ini
[Unit]
Description=Pet Memory Star Backend
After=network.target

[Service]
Type=simple
User=root
WorkingDirectory=/opt/pet-memory-star
Environment="PATH=/opt/pet-memory-star/venv/bin"
ExecStart=/opt/pet-memory-star/venv/bin/python3 /opt/pet-memory-star/start_server.py
Restart=always
RestartSec=10

[Install]
WantedBy=multi-user.target
```

启用服务：
```bash
sudo systemctl daemon-reload
sudo systemctl enable pet-memory-star
sudo systemctl start pet-memory-star
sudo systemctl status pet-memory-star
```

### 2. 配置日志轮转

防止日志文件过大：

```bash
sudo nano /etc/logrotate.d/pet-memory-star
```

内容：
```
/opt/pet-memory-star/server.log {
    daily
    missingok
    rotate 7
    compress
    delaycompress
    notifempty
    create 0644 root root
}
```

### 3. 配置监控告警

使用简单的监控脚本：

```bash
# crontab -e
*/5 * * * * /opt/check_server.sh
```

`check_server.sh`:
```bash
#!/bin/bash
if ! curl -f http://localhost:80/api/health > /dev/null 2>&1; then
    echo "$(date): Service down, restarting..." >> /opt/check.log
    systemctl restart pet-memory-star
fi
```

---

## 🆘 紧急联系方式

如果以上方法都无法解决，可以：

1. 查看服务器日志：`/opt/pet-memory-star/server.log`
2. 查看系统日志：`journalctl -xe`
3. 联系云服务商技术支持
4. 在项目仓库提交issue

---

**更新时间**: 2025-10-19  
**状态**: 待服务器检查  
**优先级**: 🔴 高

