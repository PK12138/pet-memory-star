# 🚀 微信登录功能部署指南

## 📋 服务器部署步骤

### 1️⃣ 拉取最新代码

```bash
cd /root/pet-memory-star
git pull origin main
```

### 2️⃣ 安装新的依赖

微信登录需要 `requests` 库：

```bash
cd /root/pet-memory-star/app
pip install requests
```

或使用 pip3：

```bash
pip3 install requests
```

### 3️⃣ 配置环境变量（推荐）

为了安全，建议使用环境变量存储微信密钥：

```bash
# 编辑环境变量文件
nano ~/.bashrc

# 添加以下内容
export WECHAT_APPID="wx9572f66945407446"
export WECHAT_SECRET="c4b410be644231ff5635ec960dde38c1"

# 保存并生效
source ~/.bashrc
```

### 4️⃣ 重启后端服务

#### 方式1：如果使用 systemd

```bash
sudo systemctl restart pet-memory-star
```

#### 方式2：如果使用 screen/tmux

```bash
# 查找现有进程
ps aux | grep "python.*main.py"

# 杀死旧进程
kill -9 <进程ID>

# 启动新进程
cd /root/pet-memory-star/app
nohup python main.py > /root/pet-memory-star/backend.log 2>&1 &
```

#### 方式3：如果使用 supervisor

```bash
sudo supervisorctl restart pet-memory-star
```

### 5️⃣ 验证服务是否正常

```bash
# 检查进程
ps aux | grep "python.*main.py"

# 检查端口
netstat -tlnp | grep 8000

# 查看日志
tail -f /root/pet-memory-star/backend.log

# 测试接口
curl http://localhost:8000/api/health
```

应该返回：
```json
{
  "status": "healthy",
  "message": "服务器运行正常"
}
```

### 6️⃣ 测试微信登录接口

```bash
curl -X POST http://42.193.230.145/api/auth/wx-login \
  -H "Content-Type: application/json" \
  -d '{"code":"test_code_123"}'
```

**预期响应：**
- 如果code无效：会返回微信API的错误信息（正常）
- 如果服务正常：返回JSON格式的响应（不是404）

---

## 🔍 常见问题排查

### ❌ 问题1：提示 "No module named 'requests'"

**解决：**
```bash
pip install requests
# 或
pip3 install requests
```

### ❌ 问题2：端口8000被占用

**检查占用：**
```bash
lsof -i :8000
```

**杀死占用进程：**
```bash
kill -9 <PID>
```

### ❌ 问题3：微信登录返回 404

**可能原因：**
1. 服务未重启
2. 代码未拉取
3. 路由未生效

**解决：**
```bash
# 完全停止服务
pkill -f "python.*main.py"

# 重新启动
cd /root/pet-memory-star/app
python main.py
```

### ❌ 问题4：微信API调用失败

**检查网络：**
```bash
curl https://api.weixin.qq.com/
```

**检查环境变量：**
```bash
echo $WECHAT_APPID
echo $WECHAT_SECRET
```

---

## 📝 数据库迁移（重要）

新版本修改了 users 表结构，需要执行迁移：

```bash
# 备份数据库
cd /root/pet-memory-star/app
cp pet_memorials.db pet_memorials.db.backup

# Python 交互式迁移
python3 << EOF
from database import Database
import sqlite3

# 连接数据库
conn = sqlite3.connect('pet_memorials.db')
cursor = conn.cursor()

# 添加新字段（如果不存在）
try:
    cursor.execute('ALTER TABLE users ADD COLUMN openid TEXT UNIQUE')
    print("✅ 添加 openid 字段")
except sqlite3.OperationalError as e:
    print(f"⚠️ openid 字段已存在: {e}")

try:
    cursor.execute('ALTER TABLE users ADD COLUMN nickname TEXT')
    print("✅ 添加 nickname 字段")
except sqlite3.OperationalError as e:
    print(f"⚠️ nickname 字段已存在: {e}")

try:
    cursor.execute('ALTER TABLE users ADD COLUMN phone TEXT')
    print("✅ 添加 phone 字段")
except sqlite3.OperationalError as e:
    print(f"⚠️ phone 字段已存在: {e}")

# 修改 email 和 password_hash 为可选（SQLite 不支持直接修改列属性）
# 这些字段保持原样，兼容旧数据

conn.commit()
conn.close()
print("✅ 数据库迁移完成")
EOF
```

---

## ✅ 部署完成检查清单

- [ ] 代码已拉取到最新版本
- [ ] requests 库已安装
- [ ] 环境变量已配置（可选）
- [ ] 后端服务已重启
- [ ] 服务运行正常（8000端口监听）
- [ ] 健康检查接口返回正常
- [ ] 数据库已迁移（添加新字段）
- [ ] 微信登录接口可访问（不是404）

---

## 🎉 测试微信登录

部署完成后，在小程序中测试：

1. **打开小程序** → 点击"我的"
2. **跳转到登录页** → 看到"微信一键登录"按钮
3. **点击登录按钮** → 查看控制台日志
4. **登录成功** → 自动跳转到首页
5. **查看个人中心** → 显示微信昵称和头像

---

## 📊 监控日志

```bash
# 实时查看后端日志
tail -f /root/pet-memory-star/backend.log | grep -E "wx-login|openid|登录"

# 查看错误日志
tail -f /root/pet-memory-star/backend.log | grep -E "ERROR|Failed|失败"
```

---

## 🔒 安全建议

1. ✅ 使用环境变量存储 AppSecret
2. ✅ 定期备份数据库
3. ✅ 启用 HTTPS（建议配置SSL证书）
4. ✅ 限制API访问频率
5. ✅ 记录登录日志用于审计

---

**部署遇到问题？** 
- 查看日志：`tail -f backend.log`
- 检查端口：`netstat -tlnp | grep 8000`
- 测试接口：`curl http://localhost:8000/api/health`

