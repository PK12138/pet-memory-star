# 🚨 立即执行 - 服务器修复步骤

## 问题诊断结果

✅ **小程序代码**：正常，已修复所有问题
❌ **服务器连接**：**端口80和443都无法访问**
❌ **根本原因**：**服务器上的Python服务没有运行**

## 立即操作（按顺序执行）

### 步骤1：SSH连接到服务器

```bash
ssh root@42.193.230.145
```

### 步骤2：执行一键修复脚本

```bash
bash /opt/pet-memory-star/一键修复.sh
```

**这个脚本会自动：**
1. ✅ 检查当前进程和端口状态
2. ✅ 停止所有旧进程
3. ✅ 拉取最新代码
4. ✅ 检查并激活Python环境
5. ✅ 检查依赖和数据库
6. ✅ 启动新服务
7. ✅ 测试API是否正常
8. ✅ 显示最终状态

### 步骤3：查看脚本输出

**脚本运行后，确认以下内容：**

✅ 看到 "端口80已开始监听"  
✅ 看到 "✅ 服务已启动，PID: XXXX"  
✅ 看到健康检查返回 `{"status":"healthy",...}`  

**如果看到错误，复制整个输出给我。**

### 步骤4：测试服务器（在服务器上执行）

```bash
# 测试健康检查
curl http://localhost/api/health

# 测试登录接口
curl -X POST http://localhost/api/auth/login \
  -H "Content-Type: application/json" \
  -d '{"email":"1208155205@qq.com","password":"your_password"}'
```

### 步骤5：在小程序中测试

1. **清除缓存**：
   - 开发者工具 → 清除 → 清除数据缓存
   - 重新编译

2. **测试登录**：
   - 输入账号密码
   - 点击登录
   - 观察控制台输出

## 预期结果

执行一键修复脚本后，你应该看到：

```
=== 10. 查看启动日志 ===
INFO: Uvicorn running on http://0.0.0.0:80 (Press CTRL+C to quit)
✅ 环境变量加载成功
🚀 启动 爪迹星·云纪念馆服务...

=== 11. 测试API ===
健康检查：
{
  "status": "healthy",
  "message": "服务器运行正常",
  "timestamp": "2025-10-21T..."
}

=== 12. 最终状态检查 ===
进程状态：
root  12345  ... python3 start_server.py

端口监听：
tcp   LISTEN  0  2048  0.0.0.0:80  0.0.0.0:*
```

## 如果仍然失败

### 情况1：脚本执行失败

```bash
# 查看详细错误
tail -100 /opt/pet-memory-star/nohup.out
```

### 情况2：端口被占用

```bash
# 查找占用端口的进程
lsof -i :80
# 强制停止
pkill -9 -f uvicorn
pkill -9 -f python3
# 重新执行一键修复脚本
bash /opt/pet-memory-star/一键修复.sh
```

### 情况3：云服务器安全组问题

1. 登录腾讯云控制台
2. 进入云服务器 → 安全组
3. 确认入站规则包含：
   - 端口：80，协议：TCP，源：0.0.0.0/0
   - 端口：443，协议：TCP，源：0.0.0.0/0

## 常见错误和解决方案

| 错误信息 | 原因 | 解决方案 |
|---------|------|---------|
| `ModuleNotFoundError` | Python依赖缺失 | `pip install -r requirements.txt` |
| `Address already in use` | 端口被占用 | `pkill -9 -f uvicorn` |
| `Permission denied` | 权限不足 | 使用 `sudo` 或 root 用户 |
| `Database is locked` | 数据库被锁定 | 重启服务器或删除数据库锁文件 |

## 完整的手动修复步骤（如果脚本失败）

```bash
cd /opt/pet-memory-star
git fetch --all
git reset --hard origin/main
pkill -9 -f python3
source venv/bin/activate
pip install -r requirements.txt
nohup python3 start_server.py > nohup.out 2>&1 &
sleep 5
tail -50 nohup.out
curl http://localhost/api/health
```

## 所有修复的问题清单

| 问题 | 状态 | 修复内容 |
|------|------|---------|
| 请求头不匹配 | ✅ 已修复 | 改为 `x-session-token` |
| 登录状态丢失 | ✅ 已修复 | 增强本地存储持久化 |
| API错误处理 | ✅ 已修复 | 只在401时登出 |
| 首页500错误 | ✅ 已修复 | 使用默认值防止白屏 |
| 服务器未运行 | ⏳ 待执行 | 运行一键修复脚本 |

## 联系方式

如果执行后还有问题，请提供：
1. 一键修复脚本的完整输出
2. `tail -100 /opt/pet-memory-star/nohup.out` 的输出
3. 小程序控制台的错误信息（截图）

---

**现在立即执行步骤1和步骤2！**

